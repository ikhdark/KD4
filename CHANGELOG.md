## KD4 unreleased

- Hardened retained command output so model-visible results expose opaque,
  current-task artifact IDs instead of local filesystem paths, with bounded
  follow-up reads through `read_tool_output`.

The upstream changelog can be found on the
[releases page](https://github.com/openai/codex/releases).
