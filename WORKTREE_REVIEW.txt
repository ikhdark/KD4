WORKTREE REVIEW LEDGER
======================

REVIEW METADATA
---------------
Repository/worktree: C:\Users\kuh\Desktop\kd4
Branch and baseline: main at ed3e3d78cf2ade246ab58074529113923e91bae2; origin/main matched; index clean; 57 unstaged tracked modifications; no additions, deletions, renames, copies, type changes, submodules, ignored paths, or untracked paths.
Frozen inventory rule: the 57 paths below are the exact initial non-ignored worktree baseline. WORKTREE_REVIEW.txt was clean at freeze time and is review-generated.
Attribution: implementation and test changes are task-attributed; changed AGENTS.md files are related supporting policy. No unrelated pre-existing semantic delta was identified.
Instructions read completely: C:\Users\kuh\Desktop\LOCAL-KD\skills\worktree-review\SKILL.md; AGENTS.md; SOURCEMAP.md; codex-rs/AGENTS.md; codex-rs/core/AGENTS.md; codex-rs/prompts/AGENTS.md; codex-rs/protocol/AGENTS.md; codex-rs/shell-command/AGENTS.md; codex-rs/tui/src/bottom_pane/AGENTS.md; scripts/AGENTS.md; scripts/codex_package/AGENTS.md; scripts/install/AGENTS.md.
Source closures inspected: core-agent-runtime, planning-architecture-runtime, repository-context-discovery, and tool-output-recovery source-owner slices, all untruncated with zero omitted relationships/material unknowns; justfile analyzer/test/publish recipes; cargo-nextest filterset reference.
Expected analyzers: Cargo Clippy for the Rust workspace/all targets/all features with --keep-going in the warmed lane; just cargo-shear; just rust-dead-code-matrix -- --all-features.
Expected focused proof: exact codex-core tests for repaired planning, validation, mutation, output-artifact, tool lifecycle, polling, evidence, and workspace behavior; codex-shell-command PowerShell parser test; codex-utils-output-truncation test; app-server logging test; python -m unittest scripts.test_kd4_turn_latency_audit; cargo fmt --check; git diff --check. No final full suite is required because neither repository policy nor the user requested one. Publish proof is ordered after report reconciliation, commit, and push as explicitly requested.

INITIAL FINDING SUMMARY
-----------------------
Confirmed bugs: 12. Incomplete-wiring findings: 2 (F2, F4). Contradiction findings: 2 (F1, F2). Lifecycle/concurrency findings: 4 (F6-F9). Possible findings: 0. Initial dispositions below are authoritative until the repair section is reconciled.

F1 HIGH, confidence high. codex-rs/utils/output-truncation/src/lib.rs defines success/failure/diagnostic defaults as 1,000/4,000/4,000, while both touched contract tests require 4,000/10,000/10,000. Trigger: compile/run either contract test. Observable failure: publish validation fails, and runtime token behavior differs from its tests. Smallest safe fix: retain the compatible 4,000/10,000/10,000 contract established by two independent consumers. Example: a successful command with 2,000 tokens should remain inline under the intended contract. Disposition: repair.
F2 HIGH, confidence high. codex-rs/core/src/tools/handlers/plan.rs stops automatic validation but leaves the entire private runner implementation and imports reachable only as dead code; its new test also calls plan_normalization_reason with the old arity. Trigger: compile tests/dead-code matrix. Observable failure: compile/dead-code failure and contradictory old/new validation implementations. Smallest safe fix: complete the intended non-dispatch design by removing the obsolete private runner path/imports and fix focused tests. Example: update_plan returns runner_dispatch=not_started without retaining a second hidden execution architecture. Disposition: repair.
F3 HIGH, confidence high. shell validation and validation_admission label nextest test(foo) as exact, but installed nextest documents that bare test() uses contains matching and only test(=foo) is equality. Trigger: structured route -E test(a). Observable failure: broad tests are admitted/canonicalized as exact; reuse identity can equate non-equivalent proof scopes. Smallest safe fix: require and parse test(=ID), keep directional coverage separate from symmetric identity, and cover target selectors. Example: test(a) unintentionally selects many tests. Disposition: repair.
F4 HIGH, confidence high. tools/events.rs resolves CommandMutation::Uncertain from before/after evidence, then passes only possible_mutation: bool into the generic ledger API. Into<bool> converts true back to KnownMutation, losing the new uncertain state and observed_mutation distinction. Trigger: workspace identity cannot be captured around an unknown command. Observable failure: the task ledger records an observed mutation that was never observed and skips the intended warning branch. Smallest safe fix: pass the resolved CommandMutation itself. Example: a custom inspector outside a Git repository. Disposition: repair.
F5 HIGH, confidence high. turn_diff_tracker.rs treats any $variable expression and any assignment whose RHS begins with $ as read-only, including instance method calls such as $file.Delete() or $x = $file.Delete(). Trigger: PowerShell command using an instance mutator. Observable failure: workspace mutation can bypass mutation invalidation and run under read-oriented classification. Smallest safe fix: reject variable expressions containing invocation parentheses while preserving simple indexing/slicing/property reads; add regressions. Disposition: repair.
F6 HIGH, confidence high. tools/registry.rs changes memory pollution persistence from awaited to an untracked tokio::spawn. All sibling external-context paths await the same state transition. Trigger: external-context tool output followed immediately by another sampling/resume or runtime shutdown. Observable failure: memories may be used before pollution is recorded, or the write may be lost. Smallest safe fix: restore the awaited transition. Example: a connector result followed by memory retrieval. Disposition: repair.
F7 HIGH, confidence high. tools/parallel.rs treats watcher generation as authoritative for proven-read-only evidence, while git_workspace.rs explicitly states watcher delivery is asynchronous and cannot prove no queued external edit. Trigger: external edit before the watcher receiver processes its event. Observable failure: stale workspace output can be registered current. Smallest safe fix: use content identities for authoritative before/after proof; watcher generations remain advisory. Example: an editor changes a read file during tool execution. Disposition: repair.
F8 MEDIUM, confidence high. command_output_artifact.rs caches up to 16 validated snapshots by entry count only; each logical snapshot may be 256 MiB, permitting roughly 4 GiB retained by the static cache. Trigger: read 16 distinct maximum-size artifacts. Observable failure: excessive resident memory/OOM. Smallest safe fix: byte-bound the cache and skip/evict oversized snapshots. Example: repeated read_tool_output across large build logs. Disposition: repair.
F9 MEDIUM, confidence high. write_stdin now internally repolls after actionable output until process exit or the five-minute background deadline, ignoring the caller-visible yield boundary. Trigger: long-lived process that emits progress. Observable failure: a poll that has useful output remains hidden for up to five minutes. Smallest safe fix: owner-drain only empty/whitespace observations; return actionable output promptly. Example: a build emits a progress line every few seconds. Disposition: repair.
F10 MEDIUM, confidence high. locked-user-decision supersession matches only question_id across calls, but protocol validation makes IDs unique only within one request. Trigger: two unrelated prompts both use id=choice. Observable failure: compaction hides the earlier independent decision as superseded. Smallest safe fix: require matching question identity (id, header, and question text) for supersession. Example: deployment choice followed later by formatting choice. Disposition: repair.
F11 MEDIUM, confidence high. kd4_turn_latency_audit sums multiple child wall times and subtracts that work sum from parent round-trip as if it were elapsed critical-path time, while also labeling multi-child parents parallelBatches. Trigger: Promise.all with two child calls. Observable failure: orchestration gap/share is understated or clamped and presented as exact. Smallest safe fix: report child work separately and compute an explicit gap interval from max-child versus summed-child bounds; avoid exact-share claims when topology is unknown. Example: 0.25s and 0.75s calls running concurrently inside a 3s parent. Disposition: repair.
F12 MEDIUM, confidence high. validation_admission canonicalization discards cargo --test/--bin/--example/--bench target selectors, so identical test names in different targets share an identity. Trigger: same exact test ID in integration_a and integration_b. Observable failure: one target's proof can be reused for another. Smallest safe fix: retain target selector/kind/name in the canonical route and require it for semantic equivalence. Disposition: repair with F3.

FROZEN INVENTORY PRIMARY ENTRIES
--------------------------------
Each numbered entry supplies: state; attribution; complete-review status; affected production behavior; callers/consumers; confirmed/possible findings; unfinished implementation; wiring; regression/correctness; lifecycle/concurrency; contradiction; publish risk; intended disposition.

001 AGENTS.md | M; supporting policy; complete; root repository/validation/publish/benchmark policy; all contributors and publish flow; none/none; complete; coherent with SOURCEMAP; no code regression; lifecycle n/a; no contradiction; governs final gates; retain.
002 codex-rs/AGENTS.md | M; supporting policy; complete; Rust workspace routing/invariants; Rust contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; governs Rust proof; retain.
003 codex-rs/app-server/tests/suite/logging.rs | M; supporting test; complete; structured tool timing log contract; app-server logging subscriber and ToolCallTimingGuard; none/none; complete; fields wired to trace snapshot; assertions bounded; lifecycle n/a; no contradiction; focused compile/test required; retain.
004 codex-rs/core/AGENTS.md | M; supporting policy; complete; core runtime/model-context policy; core contributors; none/none; complete; coherent; no regression; lifecycle n/a; no contradiction; governs core proof; retain.
005 codex-rs/core/src/git_workspace.rs | M; current implementation; complete; content/worktree identities and watcher observations; parallel evidence, task evidence, command mutation detection; F7/none; watcher identity optimization incomplete; new helper wired to parallel; authoritative freshness regression; asynchronous delivery race F7; contradicts its own authoritative-capture comment; blocks publish; repair F7.
006 codex-rs/core/src/session/mod.rs | M; current implementation; complete; request_user_input decision persistence; request handler/task ledger/compaction; F10/none; production write is wired; response recording wired after non-interrupted answer; cross-call identity regression F10; persistence awaited; no duplicate path; blocks decision-fidelity claim; repair F10 in ledger owner.
007 codex-rs/core/src/session/reasoning_governor.rs | M; current implementation; complete; convergence and process-monitor signals; sampling dispatch; F9 interaction noted/no direct defect; process monitor wiring complete; stable failures remain gated; write_stdin success intentionally not failure; no independent lifecycle issue; no contradiction; depends on F9; retain after F9.
008 codex-rs/core/src/session/turn.rs | M; current implementation; complete; prompt tool-search compaction and activated schemas; prompt construction/router; none/none; complete; both prompt builders wired; prior search schemas compact only after acknowledgement; Arc cloning bounded; no contradiction; focused tests/Clippy required; retain.
009 codex-rs/core/src/task_evidence.rs | M; current implementation; complete; schema v12, decision ledger, mutation receipts, compaction, validation proof matching; F4,F10,F12/none; incomplete caller state and supersession/proof identity; session/events wired but F4/F10 incomplete; correctness defects stated; persistence serialized under owner; validation canonical contradiction via F12; blocks publish; repair F4/F10/F12.
010 codex-rs/core/src/task_evidence_tests.rs | M; supporting test; complete; receipt schema compatibility; task_evidence module; F4 coverage gap/none; fixtures updated; no caller proof; no standalone regression; lifecycle n/a; no contradiction; focused tests required; extend for repairs.
011 codex-rs/core/src/tasks/mod.rs | M; current implementation; complete; exact terminal authority selection and recovery; session terminal finalization/rollout/task evidence; none/none; recovery task wired; producer/consumer complete; candidate preservation compatible; tracked retry stops on shutdown; no duplicate authority accepted; focused terminal tests required; retain.
012 codex-rs/core/src/tasks/mod_tests.rs | M; supporting test; complete; terminal authority selection; tasks owner; none/none; complete; test wired; correct; lifecycle case covered locally; no contradiction; focused test required; retain.
013 codex-rs/core/src/tool_history.rs | M; current implementation; complete; workspace tool observation/read-only classification; parallel dispatch/history invalidation; F7 dependency/none; explicit false boundary complete; wired to parallel; conservative classification; lifecycle depends on authoritative capture; no contradiction; retain after F7.
014 codex-rs/core/src/tools/code_mode/execute_handler.rs | M; current implementation; complete; code-mode timing and synchronous response formatting; code-mode runtime/registry; none/none; complete; NestedRuntime timing wired; no semantic regression found; waits remain owned; no contradiction; focused compile required; retain.
015 codex-rs/core/src/tools/code_mode/mod.rs | M; current implementation; complete; removes unnecessary async from response formatter; code-mode execute/wait; none/none; complete; both callers updated; pure function behavior unchanged; lifecycle improved; no contradiction; compile required; retain.
016 codex-rs/core/src/tools/code_mode/wait_handler.rs | M; current implementation; complete; synchronous formatter call; code-mode wait; none/none; complete; caller updated; behavior unchanged; wait evidence preserved; no contradiction; compile required; retain.
017 codex-rs/core/src/tools/command_execution.rs | M; current implementation; complete; uncertain baselines and selected-test proof; events and validation cache; F4,F3 dependency/none; state API complete but F4 caller incomplete; selected-count path wired; exact-selector semantics depend on F3; baselines removed on normal end; no contradiction; blocks publish through F3/F4; repair callers/contracts.
018 codex-rs/core/src/tools/command_output_artifact.rs | M; current implementation; complete; lazy raw artifacts, staged logical install, validated snapshot caches; exec/registry/read_tool_output/validation; F8/none; production wiring complete; all variants handled; large static cache regression F8; staged cleanup/drop paths present; no contradiction; memory publish risk; repair F8.
019 codex-rs/core/src/tools/events.rs | M; current implementation; complete; command begin/end evidence and mutation recording; shell/unified exec/task evidence/tool history; F4/none; caller incomplete; before/after captures wired; bool conversion loses state; baseline lifecycle otherwise normal; no duplicate implementation; blocks evidence correctness; repair F4.
020 codex-rs/core/src/tools/handlers/command_preflight_tests.rs | M; supporting test; complete; broad rg classification; shell preflight; none/none; complete; test wired; behavior conservative; lifecycle n/a; no contradiction; focused test required; retain.
021 codex-rs/core/src/tools/handlers/plan.rs | M; current implementation; complete; durable plan updates without auto-dispatch; update_plan/task evidence/shell validation; F2/none; obsolete runner remains; returned missing-proof wiring complete; compile/dead-code regression; cancellation before commit preserved; old/new implementations contradict; blocks publish; repair F2.
022 codex-rs/core/src/tools/handlers/plan_tests.rs | M; supporting test; complete; normalization messaging; plan handler; F2/none; old-arity call incomplete; wired module; compile failure; lifecycle n/a; no separate contradiction; blocks publish; repair F2.
023 codex-rs/core/src/tools/handlers/shell.rs | M; current implementation; complete; structured validation admission; plan/direct validation; F3,F12/none; exact matching incomplete; all callers wired; contains matcher mislabeled exact; lifecycle n/a; proof identity contradiction; blocks validation trust; repair F3/F12.
024 codex-rs/core/src/tools/handlers/shell/shell_command.rs | M; current implementation; complete; validation identity includes repository epoch; shell validation reuse; F3/F12 dependency/none; complete locally; identity inputs wired; epoch correctness improved; lifecycle unchanged; no local contradiction; depends on canonical repair; retain after F3/F12.
025 codex-rs/core/src/tools/handlers/shell_spec.rs | M; current tool contract; complete; exec default yield description; model tool schema; none/none; complete; runtime matches 2s default; correct; lifecycle n/a; no contradiction; snapshot test required; retain.
026 codex-rs/core/src/tools/handlers/shell_spec_tests.rs | M; supporting test; complete; exec schema default; shell spec; none/none; complete; aligned; correct; lifecycle n/a; no contradiction; focused test required; retain.
027 codex-rs/core/src/tools/handlers/shell_tests.rs | M; supporting test; complete; structured/direct validation forms; shell validator; F3,F12/none; tests encode contains-as-exact bug; wired; incorrect expectation; lifecycle n/a; proof contradiction; blocks publish; repair tests with implementation.
028 codex-rs/core/src/tools/handlers/tool_search.rs | M; current implementation; complete; bounded deferred-tool search/cache; router/model prompt; none/none; complete; inventory fingerprint and result builder wired; byte accounting reviewed; cache mutex excludes hashing/build; no contradiction; focused tests required; retain.
029 codex-rs/core/src/tools/handlers/tool_search_spec.rs | M; current tool contract; complete; direct exact deferred activation guidance; model/router; none/none; complete; aligned with router; correct; lifecycle n/a; no contradiction; focused test required; retain.
030 codex-rs/core/src/tools/handlers/unified_exec.rs | M; current implementation; complete; 2s exec launch yield; exec handler/spec; none/none; complete; default wired; behavior intentional; process lifecycle remains manager-owned; no contradiction; focused test required; retain.
031 codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs | M; current implementation; complete; repository epoch validation identity and lazy artifact startup; process manager/artifact writer; F1,F8 dependencies/none; complete; pending state wired; correctness depends on output contracts; artifact lifecycle reviewed; no contradiction; retain after repairs.
032 codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs | M; current implementation; complete; live-process polling and owner drain; process manager/model; F9/none; loop over-wired; merge path complete; actionable-output latency regression; hidden five-minute lifecycle; contradicts yield expectation; blocks responsiveness; repair F9.
033 codex-rs/core/src/tools/parallel.rs | M; current implementation; complete; tool gates and workspace evidence; router/tool history/git workspace; F7/none; generation shortcut unsafe; all paths wired; authoritative freshness regression; watcher race F7; contradicts git_workspace contract; blocks publish; repair F7.
034 codex-rs/core/src/tools/registry.rs | M; current implementation; complete; tool lifecycle timing, projection, memory pollution; every core tool and state DB; F6,F8 dependency/none; fire-and-forget state transition incomplete; lifecycle phases wired; memory-safety ordering regression; untracked task F6; no duplicate implementation; blocks publish; repair F6.
035 codex-rs/core/src/tools/router.rs | M; current implementation; complete; atomic deferred activation and per-turn schemas; tool dispatch/prompt; none/none; complete; activation revision verified; exact registered tool remains permission/sandbox checked; turn-local state synchronized; no contradiction; focused tests required; retain.
036 codex-rs/core/src/tools/tool_dispatch_trace.rs | M; current implementation; complete; phase timing and deferred trace persistence; registry/rollout trace/logging; none/none; complete; handler boundaries wired; telemetry-only asynchronous recording; spawned blocking closure owns data; no contradiction; logging test required; retain.
037 codex-rs/core/src/tools/tool_dispatch_trace_tests.rs | M; supporting test; complete; timing boundaries; trace owner; none/none; complete; aligned; correct; lifecycle n/a; no contradiction; focused test required; retain.
038 codex-rs/core/src/turn_diff_tracker.rs | M; current implementation; complete; command mutation classification/freshness; events/task evidence/tool history; F5/none; PowerShell classifier incomplete; new mutation enum wired except F4 caller; method invocation escape; mutation state lifecycle affected; no duplicate implementation; blocks safety; repair F5.
039 codex-rs/core/src/turn_diff_tracker_tests.rs | M; supporting test; complete; mutation observation and PowerShell reads; tracker; F5 coverage gap/none; incomplete negative cases; wired; existing cases pass conceptually; lifecycle n/a; no contradiction; add regressions.
040 codex-rs/core/src/unified_exec/process.rs | M; current implementation; complete; hides pending artifacts from outputs; unified process manager; none/none; complete; variant handled; correct; pending lifecycle remains writer-owned; no contradiction; compile required; retain.
041 codex-rs/core/src/unified_exec/process_manager.rs | M; current implementation; complete; early quiet yield; exec handler; none/none; complete; deadline reset behavior wired; correct; pause/deadline extension preserved; no contradiction; focused tests required; retain.
042 codex-rs/core/src/unified_exec/process_manager_tests.rs | M; supporting test; complete; output token defaults; process manager/output truncation; F1/none; expectation contradicts implementation; wired; compile assertion fails; lifecycle n/a; contradiction F1; blocks publish; retain test and repair owner.
043 codex-rs/core/src/validation_admission.rs | M; current implementation; complete; validation identity and proof coverage; shell/task evidence/cache; F3,F12/none; canonical route incomplete; production use wired; non-equivalent routes collide; lifecycle cache reuse affected; symmetric/directional semantics conflated; blocks proof trust; repair F3/F12.
044 codex-rs/otel/src/metrics/names.rs | M; current metric contract; complete; lifecycle duration metric; registry/telemetry exporter; none/none; complete; constant consumed; low-cardinality labels; lifecycle n/a; no contradiction; Clippy/app logging required; retain.
045 codex-rs/prompts/AGENTS.md | M; supporting policy; complete; prompt ownership; prompt contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; none; retain.
046 codex-rs/protocol/AGENTS.md | M; supporting policy; complete; wire/persistence ownership; protocol contributors; none/none; complete; coherent; supports F10 evidence that IDs are request-local; lifecycle n/a; no contradiction; none; retain.
047 codex-rs/shell-command/AGENTS.md | M; supporting policy; complete; shell safety boundary; shell contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; governs F5; retain.
048 codex-rs/shell-command/src/command_safety/powershell_parser.ps1 | M; current implementation; complete; static AST flattening of command-output assignment; PowerShell safety parser/Rust wrapper; none/none; complete; assignment/bare-read path wired; dynamic uses fail closed; helper process lifecycle unchanged; no contradiction; Windows focused test required; retain.
049 codex-rs/shell-command/src/powershell.rs | M; supporting tests; complete; PowerShell parser assignment coverage; parser subprocess; none/none; complete; tests wired Windows-only; correct; subprocess bounded; no contradiction; focused test required; retain.
050 codex-rs/tui/src/bottom_pane/AGENTS.md | M; supporting policy; complete; bottom-pane scope; TUI contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; none; retain.
051 codex-rs/utils/output-truncation/src/lib.rs | M; current implementation; complete; default model output budgets; all command/tool projection; F1/none; contract incomplete; callers wired; incompatible constants; lifecycle n/a; contradicts two tests; blocks publish; repair F1.
052 codex-rs/utils/output-truncation/src/truncate_tests.rs | M; supporting test; complete; output budget contract; truncation crate; F1/none; complete expectation; wired; correct intended compatibility; lifecycle n/a; contradicts implementation; focused test required; retain.
053 scripts/AGENTS.md | M; supporting policy; complete; scripts/publish/performance policy; script contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; governs publish/benchmark; retain.
054 scripts/codex_package/AGENTS.md | M; supporting policy; complete; package contract; package contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; none; retain.
055 scripts/install/AGENTS.md | M; supporting policy; complete; standalone install contract; installer contributors; none/none; complete; coherent; no code regression; lifecycle n/a; no contradiction; none; retain.
056 scripts/kd4_turn_latency_audit.py | M; current implementation; complete; rollout latency/orchestration report schema v2; audit users/fixtures; F11/none; topology semantics incomplete; parser wired; exact-gap claim incorrect; snapshot lifecycle remains fixed-length; work/elapsed contradiction; benchmark report not publish-trustworthy; repair F11.
057 scripts/test_kd4_turn_latency_audit.py | M; supporting test; complete; report schema/orchestration metrics; audit script; F11/none; test encodes exact-sum error; wired; expected gap incorrect for possible parallelism; lifecycle n/a; contradiction with parallelBatches; focused unittest required; repair F11.

INVENTORY RECONCILIATION BEFORE REPAIR
--------------------------------------
Git freeze count: 57. Numbered primary entries: 57. Each exact frozen path appears once; no directory substitutes a path. WORKTREE_REVIEW.txt is separately review-generated. Every confirmed finding has a repair disposition; there are no unresolved possible findings. No repair has yet been made. This filesystem ledger must be reread completely before edits.

REPAIR-GENERATED PATHS
----------------------
WORKTREE_REVIEW.txt | review-generated ledger; initial inventory, complete findings, repair dispositions, analyzer results, validation results, and final reconciliation recorded. No repository file was added, deleted, moved, or renamed by the review.

REPAIR DISPOSITIONS
-------------------
F1 FIXED. Restored the 4,000 success / 10,000 failure / 10,000 diagnostic output-token contract in codex-utils-output-truncation, matching both changed consumer tests.
F2 FIXED. Removed the obsolete private bound-validation runner and dead imports from the plan handler; updated the normalization test to the current four-argument contract.
F3 FIXED. Changed nextest exact filters and all exact-filter diagnostics/tests from contains syntax test(foo) to equality syntax test(=foo).
F4 FIXED. Preserved the resolved CommandMutation value through events into task-evidence recording instead of converting Uncertain into KnownMutation through bool.
F5 FIXED. PowerShell local-variable reads remain recognized, while method calls, property assignments, and variable RHS expressions containing invocation syntax fail closed as mutations; added all three negative regression cases.
F6 FIXED. Memory-pollution persistence is awaited in the registry lifecycle rather than detached in an untracked Tokio task.
F7 FIXED. Removed the watcher-generation authority shortcut. Parallel workspace evidence now uses full content identities before and after the call and requires equality with no host mutation advance.
F8 FIXED. Added a 64 MiB aggregate byte cap to the validated-snapshot cache, retained entry-count eviction, skipped oversized snapshots, maintained exact retained-byte accounting, and added a bounded cache regression test.
F9 FIXED. Background owner-drain continues only for empty/ASCII-whitespace observations; actionable output returns at the caller-visible yield boundary. The progress-output regression expectation was inverted accordingly.
F10 FIXED. Locked-decision supersession now requires matching id, header, and question text. The persistence test proves an unrelated later prompt reusing the same request-local id remains independent.
F11 FIXED. Latency report schema is v3. Child durations are reported as work, and orchestration gap/share are explicit lower/upper bounds derived from summed-child and max-child limits. The fixture proves the two-child interval.
F12 FIXED. Canonical validation routes retain Cargo target selectors, target flags, and harness arguments; semantic coverage requires equality for those dimensions. Added same-test-id/different-integration-target rejection coverage.

Issues fixed: 12. Unresolved confirmed or possible worktree findings: 0. Formatting-only repair: rustfmt-normalized two frozen files (session/turn.rs and tools/router.rs).

ANALYZER, VALIDATION, AND FINAL RECONCILIATION
----------------------------------------------
The ledger was reread completely before the first functional repair. The required analyzers were then run sequentially.

Clippy: cargo clippy --workspace --all-targets --all-features --keep-going. The first shell capture timed out while its Cargo children continued; the single warmed-lane retry completed far enough to report one error and then stopped: unchanged codex-rs/codex-api/src/endpoint/responses_websocket.rs:257 violates clippy::needless_borrow. Worktree-attributable Clippy findings: 0. Unrelated Clippy findings: 1. Because codex-api failed, dependent workspace/all-target/all-feature Clippy coverage did not complete. Per repository validation-failure policy, the unrelated source was not changed and Clippy was not rerun.

Cargo-shear: just cargo-shear reported one unchanged unlinked file, codex-rs/tools/src/function_call_error_tests.rs. Worktree-attributable cargo-shear findings: 0. Unrelated cargo-shear findings: 1. The unrelated file was not deleted or suppressed.

Dead-code matrix: just rust-dead-code-matrix -- --all-features could not reach dead-code diagnostics. Both the initial run and the single warmed-lane retry failed in the v8 149.2.0 build script because https://github.com/denoland/rusty_v8/releases/download/v149.2.0/rusty_v8_ptrcomp_sandbox_release_x86_64-pc-windows-msvc.lib.gz returned HTTP 404. Worktree-attributable dead-code findings observed: 0; unrelated dead-code findings observed: 0; analyzer completion: blocked by infrastructure before analysis. No cold lane was created.

Passing validation:
- python -m unittest scripts.test_kd4_turn_latency_audit: 3 tests passed.
- cargo fmt --all -- --check: passed after the recorded formatting repair.
- git diff --check: passed (Git emitted only configured LF-to-CRLF worktree notices).
- Targeted static inspection confirmed removal of the obsolete validation runner, watcher-generation authority path, detached memory-pollution task, contains-as-exact filter spellings, and exact-gap latency fields.

Checks not run: focused Rust tests for the twelve repairs and any broader Rust/full suite. After the same dead-code infrastructure blocker occurred twice, repository policy permitted only non-building checks. Existing narrower Python proof remains valid. No unit-test or build duration is presented as a runtime benchmark; no latency or token performance claim is made beyond the corrected audit semantics.

Final inventory: 57 frozen tracked paths, each represented exactly once above, plus review-generated WORKTREE_REVIEW.txt = 58 modified tracked paths. No additions, deletions, renames, copies, type changes, submodule changes, or untracked files. Since no repository path changed identity, just source-map-check was not required.

Publish readiness: source review findings are repaired and non-building checks pass, but Rust validation and a new-binary build are not proven because of the unrelated Clippy failure and repeated V8 archive 404. Commit/push is authorized by the user and skill. Republish remains blocked under validation-failure policy until a build can run without violating the post-repeat non-building restriction.

POST-COMMIT CORRECTION AFTER PUBLISH CHECK
------------------------------------------
The first publish attempt exposed two worktree-attributable diagnostics that the earlier workspace Clippy run had not reached because codex-api failed first: an unused non-test Arc import in plan.rs and a CommandMutation move while mutation_paths still borrowed it in events.rs. The import is now test-only, and events passes the owned mutation as the sole path authority without the redundant borrowed fallback.

At the user's direction, every diagnostic subsequently exposed by focused Clippy was repaired. The codex-api needless borrow was removed. The obsolete auto-validation binding type, map, methods, and unreachable shell/events branches left behind by F2 were deleted. Test-only constructors/helpers are cfg(test). Unused workspace-evidence timing wrappers were removed. The PowerShell redundant closure and validation canonicalizer collapsible match were corrected. The workspace operation gate now uses an infallible owned Tokio mutex guard instead of expecting on semaphore acquisition.

The cargo-shear finding was also resolved: codex-rs/tools/src/function_call_error_tests.rs was an unlinked obsolete test for ToolFailureDiagnostic and ToolFailureClass, APIs no longer present in codex-tools, so it was deleted. Required source-map reconciliation passed and updated SOURCEMAP.md from 5,413 to 5,412 tracked paths.

Correction validation:
- cargo clippy -p codex-api -p codex-core --lib --keep-going: passed with zero warnings and zero errors.
- cargo fmt --all -- --check: passed.
- just cargo-shear: passed with no issues.
- just source-map-check: passed.

The historical analyzer narrative above records the first review attempt. This correction section is authoritative for the repaired state and supersedes its Clippy, cargo-shear, and publish-readiness conclusions.

FULL-SUITE CLIPPY AND FINAL RECONCILIATION
------------------------------------------
The user explicitly expanded the correction scope to every diagnostic found by the complete Clippy suite. The authoritative command was cargo clippy --workspace --all-targets --all-features --keep-going. Its initial passes exposed additional all-target fixture drift and denied lints in codex-app-server, codex-app-server-client, codex-core, codex-features, and codex-tui. All exposed diagnostics were repaired: fixtures now match the current protocol fields, exhaustive RawOutputArtifact handling includes Pending, test target selectors and diagnostics retain the exact equality contract, the task-evidence lock is dropped before await, redundant ownership and output/debug constructs were removed, and the code-mode recovery reserve is compatible with the 4,000-token success-output budget. No Clippy finding was suppressed.

The all-features v8 build required the ptrcomp+sandbox archive and generated binding from the official OpenAI rusty-v8 v149.2.0 release. Those locations were supplied only as process-local environment variables; no repository dependency or machine-wide configuration was changed. The final authoritative Clippy command passed for the entire workspace, all targets, and all features with zero warnings and zero errors.

Final analyzer and proof results:
- cargo clippy --workspace --all-targets --all-features --keep-going: passed.
- just cargo-shear: passed with no issues; the sole stale unlinked test file was deleted rather than suppressed.
- just rust-dead-code-matrix: passed across the complete workspace/all targets with -D dead_code. The only emitted notice was a future-incompatibility warning for dependency proc-macro-error2 v2.0.1, not a source or dead-code finding.
- Targeted nextest proof: 18 of 18 selected codex-core/codex-features tests passed, 3,906 tests were filtered out, run ID 56fb32e0-e96a-49fe-a739-fa377185dad4.
- cargo fmt --all -- --check: passed.
- just source-map-check: passed after the stale file deletion and retained the corrected 5,412-path inventory.
- git diff --check: passed; Git emitted only configured LF-to-CRLF worktree notices.

Correction inventory at final reconciliation: 27 review-owned modified/deleted paths, plus the user's separate unstaged AGENTS.md edit. The AGENTS.md edit is deliberately excluded from this correction commit. All analyzer, compile-contract, and targeted-test findings encountered during the correction were fixed; unresolved confirmed or possible findings: 0. Publish readiness: clean full-suite Clippy, clean dead-code and dependency analyzers, clean formatting/diff/source-map checks, and 18/18 targeted tests. Commit, push, and republish remain the authorized final actions.
