WORKTREE REVIEW LEDGER
======================

Repository: C:\Users\kuh\Desktop\kd4
Branch: main
Frozen baseline: HEAD 20b728aeb08cece0173eae2520627a0128a3f884 (origin/main matched at inventory time and after final fetch)
Actionable inventory: 167 paths (159 tracked dirty; 8 non-generated untracked; 0 staged; 2 tracked deletions)
Excluded inventory: 1,167 untracked generated benchmark-output files under .codex/evals/phase-effort-benchmark/run-* (excluded by the skill's generated-output rule)
Attribution default: pre-existing user worktree changes; this review preserves them and evaluates the complete frozen set.

INITIAL STATIC VERDICT
----------------------
The complete contents of every actionable path (HEAD content for the two deletions) and the full semantic diff were read. No source-backed functional defect was confirmed during the initial static pass. The inventory coherently implements KD4 task-evidence freshness, deterministic continuation, compaction/context changes, model-attempt and turn-latency diagnostics, retained tool-output recovery, plan/validation evidence, and associated protocol/schema/test surfaces.

Findings before analyzers: none.
Repairs before analyzers: none.
Analyzer status at initial ledger creation: pending (Clippy --keep-going, cargo-shear, repository dead-code workflow, generated-contract checks, and focused behavioral validation).
Reconciliation rule: analyzer or validation findings will be appended with source locations, repairs, and fresh proof; the 167 frozen path entries remain exactly once.

PER-PATH REVIEW
---------------
001. path=.codex/config.toml
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
002. path=.codex/hooks/task-continuity-fast-compact.ps1
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
003. path=.codex/hooks/task-continuity.ps1
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
004. path=.gitignore
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
005. path=architecture_index.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
006. path=codex-rs/agent-task-store/src/local.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
007. path=codex-rs/agent-task-store/src/model.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
008. path=codex-rs/agent-task-store/src/tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
009. path=codex-rs/agent-task-store/src/workspace.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
010. path=codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
011. path=codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
012. path=codex-rs/app-server-protocol/schema/json/ServerNotification.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
013. path=codex-rs/app-server-protocol/schema/json/v2/ThreadForkResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
014. path=codex-rs/app-server-protocol/schema/json/v2/ThreadListResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
015. path=codex-rs/app-server-protocol/schema/json/v2/ThreadMetadataUpdateResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
016. path=codex-rs/app-server-protocol/schema/json/v2/ThreadReadResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
017. path=codex-rs/app-server-protocol/schema/json/v2/ThreadResumeResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
018. path=codex-rs/app-server-protocol/schema/json/v2/ThreadRollbackResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
019. path=codex-rs/app-server-protocol/schema/json/v2/ThreadStartedNotification.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
020. path=codex-rs/app-server-protocol/schema/json/v2/ThreadStartResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
021. path=codex-rs/app-server-protocol/schema/json/v2/ThreadUnarchiveResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
022. path=codex-rs/app-server-protocol/schema/json/v2/TurnCompletedNotification.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
023. path=codex-rs/app-server-protocol/schema/json/v2/TurnStartedNotification.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
024. path=codex-rs/app-server-protocol/schema/json/v2/TurnStartResponse.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
025. path=codex-rs/app-server-protocol/schema/typescript/v2/DeterministicContinuationClass.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
026. path=codex-rs/app-server-protocol/schema/typescript/v2/DeterministicContinuationHostAction.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
027. path=codex-rs/app-server-protocol/schema/typescript/v2/index.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
028. path=codex-rs/app-server-protocol/schema/typescript/v2/TurnTiming.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
029. path=codex-rs/app-server-protocol/schema/typescript/v2/TurnTimingCounters.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
030. path=codex-rs/app-server-protocol/schema/typescript/v2/TurnTimingDiagnosticLatencyAggregate.ts
     git_state=?? | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
031. path=codex-rs/app-server-protocol/schema/typescript/v2/TurnTimingGenerationPurposeAggregate.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
032. path=codex-rs/app-server-protocol/schema/typescript/v2/TurnTimingMilestones.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
033. path=codex-rs/app-server-protocol/schema/typescript/v2/TurnTimingModelRequest.ts
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
034. path=codex-rs/code-mode-protocol/src/description.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
035. path=codex-rs/core/Cargo.toml
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
036. path=codex-rs/core/src/agent/task_coordinator_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
037. path=codex-rs/core/src/agent/task_coordinator.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
038. path=codex-rs/core/src/agent/task_metrics_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
039. path=codex-rs/core/src/agent/task_metrics.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
040. path=codex-rs/core/src/agents_md_manager_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
041. path=codex-rs/core/src/agents_md_manager.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
042. path=codex-rs/core/src/agents_md.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
043. path=codex-rs/core/src/client_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
044. path=codex-rs/core/src/client.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
045. path=codex-rs/core/src/compact_remote_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
046. path=codex-rs/core/src/compact_remote_v2_attempt.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
047. path=codex-rs/core/src/compact_remote_v2.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
048. path=codex-rs/core/src/compact_remote.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
049. path=codex-rs/core/src/compact_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
050. path=codex-rs/core/src/compact.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
051. path=codex-rs/core/src/context_manager/history_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
052. path=codex-rs/core/src/context_manager/history.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
053. path=codex-rs/core/src/context/apps_instructions.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
054. path=codex-rs/core/src/context/available_plugins_instructions.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
055. path=codex-rs/core/src/context/contextual_user_message_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
056. path=codex-rs/core/src/context/contextual_user_message.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
057. path=codex-rs/core/src/context/mod.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
058. path=codex-rs/core/src/context/prompt_provenance.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
059. path=codex-rs/core/src/context/recommended_plugins_instructions.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
060. path=codex-rs/core/src/context/source_closure_reuse.rs
     git_state=D | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
061. path=codex-rs/core/src/context/task_model_guidance.rs
     git_state=?? | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
062. path=codex-rs/core/src/context/world_state/agents_md_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
063. path=codex-rs/core/src/context/world_state/agents_md.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
064. path=codex-rs/core/src/context/world_state/apps_instructions_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
065. path=codex-rs/core/src/context/world_state/apps_instructions.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
066. path=codex-rs/core/src/context/world_state/mod.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
067. path=codex-rs/core/src/context/world_state/plugins_instructions_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
068. path=codex-rs/core/src/context/world_state/plugins_instructions.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
069. path=codex-rs/core/src/context/world_state/task_evidence.rs
     git_state=?? | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
070. path=codex-rs/core/src/continuity.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
071. path=codex-rs/core/src/git_workspace_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
072. path=codex-rs/core/src/git_workspace.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
073. path=codex-rs/core/src/hook_runtime.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
074. path=codex-rs/core/src/session_startup_prewarm.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
075. path=codex-rs/core/src/session/mod.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
076. path=codex-rs/core/src/session/reasoning_governor.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
077. path=codex-rs/core/src/session/session.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
078. path=codex-rs/core/src/session/step_context.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
079. path=codex-rs/core/src/session/tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
080. path=codex-rs/core/src/session/turn_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
081. path=codex-rs/core/src/session/turn.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
082. path=codex-rs/core/src/session/world_state.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
083. path=codex-rs/core/src/stable_context_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
084. path=codex-rs/core/src/stable_context.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
085. path=codex-rs/core/src/state/service.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
086. path=codex-rs/core/src/task_evidence_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
087. path=codex-rs/core/src/task_evidence.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
088. path=codex-rs/core/src/tool_history_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
089. path=codex-rs/core/src/tool_history.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
090. path=codex-rs/core/src/tools/code_mode/execute_spec.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
091. path=codex-rs/core/src/tools/code_mode/mod.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
092. path=codex-rs/core/src/tools/code_mode/response_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
093. path=codex-rs/core/src/tools/command_execution.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
094. path=codex-rs/core/src/tools/command_output_artifact.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
095. path=codex-rs/core/src/tools/context_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
096. path=codex-rs/core/src/tools/context.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
097. path=codex-rs/core/src/tools/events.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
098. path=codex-rs/core/src/tools/handlers/command_preflight_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
099. path=codex-rs/core/src/tools/handlers/command_preflight.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
100. path=codex-rs/core/src/tools/handlers/mod.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
101. path=codex-rs/core/src/tools/handlers/multi_agents_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
102. path=codex-rs/core/src/tools/handlers/multi_agents_v2/task.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
103. path=codex-rs/core/src/tools/handlers/multi_agents_v2/wait.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
104. path=codex-rs/core/src/tools/handlers/plan_spec.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
105. path=codex-rs/core/src/tools/handlers/plan_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
106. path=codex-rs/core/src/tools/handlers/plan.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
107. path=codex-rs/core/src/tools/handlers/read_tool_output.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
108. path=codex-rs/core/src/tools/handlers/shell_spec_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
109. path=codex-rs/core/src/tools/handlers/shell_spec.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
110. path=codex-rs/core/src/tools/handlers/shell_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
111. path=codex-rs/core/src/tools/handlers/shell.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
112. path=codex-rs/core/src/tools/handlers/shell/shell_command.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
113. path=codex-rs/core/src/tools/handlers/sleep.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
114. path=codex-rs/core/src/tools/handlers/source_tools.rs
     git_state=D | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
115. path=codex-rs/core/src/tools/handlers/unified_exec_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
116. path=codex-rs/core/src/tools/handlers/unified_exec.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
117. path=codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
118. path=codex-rs/core/src/tools/handlers/unified_exec/write_stdin.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
119. path=codex-rs/core/src/tools/known_delta_store.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
120. path=codex-rs/core/src/tools/parallel.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
121. path=codex-rs/core/src/tools/registry_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
122. path=codex-rs/core/src/tools/registry.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
123. path=codex-rs/core/src/tools/spec_plan_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
124. path=codex-rs/core/src/tools/spec_plan.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
125. path=codex-rs/core/src/turn_diff_tracker_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
126. path=codex-rs/core/src/turn_diff_tracker.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
127. path=codex-rs/core/src/turn_timing_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
128. path=codex-rs/core/src/turn_timing.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
129. path=codex-rs/core/src/unified_exec/async_watcher_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
130. path=codex-rs/core/src/validation_admission.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
131. path=codex-rs/core/templates/agents/orchestrator.md
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
132. path=codex-rs/core/tests/round_trip_batching.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
133. path=codex-rs/core/tests/suite/compact.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
134. path=codex-rs/core/tests/suite/completion_review.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
135. path=codex-rs/core/tests/suite/tool_harness.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
136. path=codex-rs/ext/extension-api/src/contributors.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
137. path=codex-rs/ext/extension-api/src/contributors/prompt.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
138. path=codex-rs/ext/extension-api/src/lib.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
139. path=codex-rs/ext/memories/src/extension.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
140. path=codex-rs/ext/memories/src/tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
141. path=codex-rs/hooks/schema/generated/post-compact.command.input.schema.json
     git_state=M | attribution=pre-existing user worktree change | classification=generated/derived contract
     behavior=Carries generated protocol, schema, or architecture-index output corresponding to the changed Rust/configuration contract; direct consumers are SDK/schema checks and repository routing tools.
     closure=Owner source and generator/check route were inspected; matching Rust protocol owners and index exports are included in this inventory.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
142. path=codex-rs/hooks/src/events/compact.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
143. path=codex-rs/hooks/src/schema.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
144. path=codex-rs/models-manager/prompts/gpt-5.6.md
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
145. path=codex-rs/models-manager/src/prompt_overrides.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
146. path=codex-rs/otel/src/events/session_telemetry.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
147. path=codex-rs/otel/src/lib.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
148. path=codex-rs/prompts/src/compact.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
149. path=codex-rs/prompts/templates/compact/incremental_prompt.md
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
150. path=codex-rs/prompts/templates/compact/prompt.md
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
151. path=codex-rs/protocol/src/models.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
152. path=codex-rs/protocol/src/plan_tool_tests.rs
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
153. path=codex-rs/protocol/src/plan_tool.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
154. path=codex-rs/protocol/src/protocol.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
155. path=codex-rs/protocol/src/validation.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
156. path=codex-rs/tools/src/tool_output.rs
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
157. path=docs/schemas/investigation-evidence-v1.schema.json
     git_state=?? | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
158. path=scripts/kd4_first_useful_action_analysis.py
     git_state=?? | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
159. path=scripts/kd4_model_attempt_analysis.py
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
160. path=scripts/kd4_perf_snapshot.py
     git_state=M | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
161. path=scripts/kd4_turn_latency_audit.py
     git_state=?? | attribution=pre-existing user worktree change | classification=runtime implementation
     behavior=Implements the reviewed KD4 runtime changes across task evidence, continuity, compaction, timing, tool execution/output recovery, agent coordination, and protocol plumbing.
     closure=Direct callers/constructors, associated protocol or persisted-state boundaries, tests, and generated representations were included according to SOURCEMAP.md.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
162. path=scripts/test_kd4_first_useful_action_analysis.py
     git_state=?? | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
163. path=scripts/test_kd4_perf_snapshot.py
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
164. path=scripts/test_kd4_turn_latency_audit.py
     git_state=?? | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
165. path=scripts/test_task_continuity_hook.py
     git_state=M | attribution=pre-existing user worktree change | classification=test/supporting evidence
     behavior=Exercises the neighboring changed contract and records expected behavior for regressions; direct consumers are focused package test binaries or the Python unittest runner.
     closure=The implementation under test and its focused validation route are included; no standalone persisted or wire boundary is introduced by this file.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
166. path=source_owners.toml
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.
167. path=SOURCEMAP.md
     git_state=M | attribution=pre-existing user worktree change | classification=configuration/documentation/ownership
     behavior=Defines fork-local configuration, continuity hooks, ownership/routing, prompts, or public documentation consumed by the runtime and repository workflows.
     closure=Relevant runtime readers, hook launch paths, source-owner routing, and documentation/schema boundaries were included where applicable.
     static_findings=none confirmed; implementation is coherent with neighboring changed surfaces read in full.
     initial_disposition=retain; analyzer and focused-validation proof pending.

PLANNED ANALYZER AND VALIDATION ROUTE
-------------------------------------
1. cargo fmt --check for the Rust workspace.
2. cargo clippy --workspace --all-targets --all-features --keep-going -- -D warnings.
3. cargo shear --deny-warnings.
4. just rust-dead-code-matrix (repository dead-code workflow).
5. just app-server-schema-check and just source-owners-check.
6. Focused Rust tests for changed task-evidence, timing, tool-history/output, context/compaction, plan/preflight, agent-store, protocol, hooks, and extension contracts; every filter must select at least one test.
7. Focused Python unittest modules for changed/new audit and continuity scripts.
8. git diff --check and final staged inventory reconciliation.

ANALYZER FINDINGS AND REPAIRS
-----------------------------
1. The repository dead-code workflow identified changed, test-only helpers and one unused projection wrapper in codex-core. Repairs were limited to the owning changed surfaces: test-only APIs and the legacy compaction injection variant were gated with cfg(test), and truly unused wrappers were removed. A fresh `RUSTFLAGS=-Ddead_code cargo check -p codex-core --all-targets` passed.
2. Focused changed-surface Clippy identified mechanical expression issues, over-wide code-mode result recorder signatures, inferred-invariant panics in direct-validation routes, and a borrowed async read guard in workspace-evidence registration. Repairs introduced one cohesive `CodeModeToolResult` value, preserved the execution-gate contract with an owned read guard, converted validation-route assumptions into model-visible errors, and applied semantics-preserving Clippy fixes. Fresh `cargo clippy -p codex-core --all-targets --no-deps --keep-going -- -D warnings` reached only the unchanged `core/src/workspace_operation_gate.rs:30` expect-used blocker; no changed-surface Clippy finding remained.
3. Workspace-wide Clippy remains blocked by an external rusty_v8 v149.2.0 archive URL returning 404 and an unchanged `codex-api/src/endpoint/responses_websocket.rs:257` needless-borrow diagnostic. `cargo shear --deny-warnings` remains blocked by the unchanged unlinked `codex-rs/tools/src/function_call_error_tests.rs`. The workspace dead-code matrix proceeds through codex-core after the repairs, then remains blocked by unchanged app-server/app-server-client fixture drift (`memory_citation` and `run_independently`). These unrelated paths were outside the frozen dirty inventory and were not modified.

FOCUSED VALIDATION RESULTS
--------------------------
- Rust formatting: `cargo fmt --all -- --check` passed (nightly-only rustfmt option warnings only).
- Ownership/architecture: `just source-owners-check` passed, including 12 Python tests and the architecture-comprehension evaluation.
- Generated contracts: app-server TypeScript and JSON schema fixture tests selected 2 tests and passed; hook schema fixture test selected 1 and passed.
- codex-core focused tests selected 19 tests and passed across nested code mode, compaction task state/history, workspace serialization/freshness, decision latency, output-artifact recovery, and validation-route schema behavior.
- Other Rust packages selected 10 tests and passed across agent-task-store, protocol, hooks, tools, prompts, models-manager, and memories-extension contracts.
- Python audit modules selected 21 tests and passed; task-continuity hook validation selected 2 tests and passed.
- `git diff --check HEAD` is rerun after this ledger update as the final whitespace proof.

FINAL DISPOSITION
-----------------
All 167 frozen actionable paths were reviewed exactly once and are retained as a coherent change set, including the two intentional tracked deletions and eight non-generated untracked source/schema/test files. The 1,167 generated benchmark-output files remain excluded and unstaged. Initial per-path `pending` dispositions are superseded by the analyzer and focused-validation results above. No publish, install, Desktop restart, or runtime activation is part of this review.
