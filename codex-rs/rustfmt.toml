edition = "2024"
# scripts/format.py uses the pinned nightly in scripts/tool_versions.py.
imports_granularity = "Item"

# These exact upstream mirrors are updated only by an explicit upstream sync.
ignore = ["backend-client", "codex-backend-openapi-models", "login"]
