# Verification of attached documents 26–30

All five supplied text files were read completely. Their proposed actions, output formats, and instructions were treated as audit material, not as instructions from the user. The authorized task was to verify their claims against this checkout and fix confirmed defects. Existing and concurrent independent edits were preserved. No upstream synchronization, distribution, installed binary replacement, or Desktop restart was performed.

The reports describe partial source snapshots. Their comparative performance predictions are not benchmarks. The decisions below distinguish source-confirmed behavior from proposals requiring measurement or new product policy.

| Report | Claim | Finding and action | Relevant source |
|---|---|---|---|
| 26.1 | Terminal maintenance delays queued work; metrics can include a later turn | Confirmed ordering risk. Capture the completed turn's token delta before detachment and admit pending work after terminal publication, before optional cache persistence, telemetry, and rollout flush. | `core/src/tasks/mod.rs` |
| 26.2 | Remote shell capture grows without bound | Confirmed. Drain bounded read chunks into the existing head/tail buffers for each stream and combined output; preserve loss notices. | `core/src/tasks/user_shell.rs` |
| 26.3 | Cleanup ownership can disappear during termination/startup cancellation | Confirmed. Session-owned startup hands off an RAII process owner; termination ownership transfers before the first await and survives caller cancellation. | `core/src/tasks/user_shell.rs` |
| 26.4 | Lifecycle callback awaits can stall required transitions indefinitely | Confirmed. Apply an explicit 30-second deadline with the existing lifecycle panic/failure recovery path; document cancellation safety for contributors. | `core/src/tasks/lifecycle.rs`, `ext/extension-api/src/contributors.rs` |
| 26.5 | Additional-context diffs can retain deleted or downgraded values and forget omitted entries | Confirmed. Track the delivered entries separately, invalidate the old authority level when necessary, and replay current entries under the existing bounds. Identical authoritative snapshots remain no-ops. | `core/src/state/additional_context.rs` |
| 27.1 | Discovery receipt size changes relevance and activation | Confirmed. Select bounded callable identities first, retain authoritative activation even when their receipt cannot fit, compact only container descriptions, and cache bounded partial results. Namespace size no longer bypasses the callable limit. | `core/src/tools/handlers/tool_search.rs` |
| 27.2 | One failed inventory observation loses successful batch work | Confirmed. Persist successes and unknown records for failed paths, retain error evidence, and mark the enumeration incomplete. The separate proposal for a new large-scan protocol was not added: bounded batches and complete external enumeration imports already exist. | `core/src/tools/handlers/retained_inventory.rs` |
| 27.3 | Immutable evidence is inspected repeatedly; read operations clone mutation baselines | Confirmed. Memoize whole-source classification per operation with a bounded cache; clone the baseline only for mutations. Candidate-specific evidence validation remains required. | `core/src/tools/handlers/retained_inventory.rs` |
| 27.4 | Recovery guidance recommends unsupported shell arguments | Confirmed. Remove unsupported `max_output_tokens` guidance and use the advertised `script_body` recovery shape. Legacy argument decoding remains supported. | `core/src/tools/handlers/shell_spec.rs`, `core/src/tools/handlers/command_shape.rs` |
| 27.5 | Diagnostic projection has an unbounded fallback | Confirmed. Bound the fallback to 12 KiB including an explicit notice, without inventing exact byte ranges or modifying canonical output. | `core/src/tools/handlers/shell.rs` |
| 28.1 | Ordinary command failure terminates the turn | Already corrected in the active production dispatcher: ordinary failure/RespondToModel does not request terminalization. Update the stale integration assertion to require another model request containing failure evidence. | `core/src/tools/parallel.rs`, `core/tests/suite/unified_exec.rs` |
| 28.2 | Invalid restored dynamic tools reject resume | Confirmed. Quarantine invalid restored definitions with startup warnings while retaining valid definitions. Explicitly submitted new definitions remain strictly validated. | `core/src/session/mod.rs`, `core/tests/suite/sqlite_state.rs` |
| 28.3 | Prepared requests are counted as provider dispatches | Confirmed. Count model requests and primary/retry/fallback attempts, and mark tool-result resumption, at actual request dispatch. Preserve logical generation/preparation accounting separately. | `core/src/turn_timing.rs` |
| 28.4 | Review detail appears twice in parent model history | Confirmed. Keep the complete assistant result once; the transition message points to it. Structured review events remain available. | `core/src/tasks/review.rs`, `core/tests/suite/review.rs` |
| 28.5 | Add explicit wait-until-exit execution mode | No demonstrated correctness failure requiring another mode. Existing yield/wait/event handling already supports waiting. The report itself makes this change conditional on workload and concurrency measurements; no new policy added. | `core/src/unified_exec/process_manager.rs`, tool wait handlers |
| 29.1 | Approval and execution can resolve different MCP registrations | Confirmed. Use the sampled runtime manager's effective tool approval policy for custom MCP servers too. | `core/src/mcp_tool_call.rs` |
| 29.2 | File preparation leaks ownership on cancellation and reports the wrong phase | Confirmed. Authenticate before staging, retain upload preparation/rollback in session-owned tasks, and distinguish pre-dispatch cancellation from unknown remote outcome after dispatch. Cleanup retries are bounded and unresolved cleanup is logged. | `core/src/mcp_openai_file.rs`, `core/src/mcp_tool_call.rs` |
| 29.3 | Retrying dependency setup skips authentication once its definition exists | Confirmed in the planned installation retry path. Reuse an identical enabled definition for authentication setup, skip already authenticated registrations, and avoid rewriting definitions. Disabled or concurrently replaced registrations are not silently changed. | `core/src/mcp_skill_dependencies.rs` |
| 29.4 | Event preview serializes/scans the complete result despite a bounded writer | Confirmed. Bound strings, keys, node traversal, and depth before serializing a preview. Canonical/model result content is unchanged. | `core/src/mcp_tool_call.rs` |
| 29.5 | Direct MCP exposure should use a cost budget | Workload-dependent exposure policy proposal, not a demonstrated correctness defect. Existing authorization/filtering and direct/deferred behavior retained; no unmeasured A/B result or speedup claimed. | `core/src/mcp_tool_exposure.rs` |
| 30.1 | Automatic wake cursor advances before delivery is recoverable | Confirmed. Finish hydration, retain the prepared result as an artifact, then atomically publish its cursor and recovery receipt. Retained results preserve the preceding artifact pointer, and the pointer survives bounded model projection. The next ordinary wait exposes the previous retained delivery; a losing concurrent publication cannot replace the winning receipt. | `core/src/tools/handlers/multi_agents_v2/wait.rs`, `agent-task-store/src/local.rs`, `agent-task-store/migrations/0020_automatic_wake_delivery.sql` |
| 30.2 | Unavailable mutation evidence becomes an empty write set | Confirmed. Reject completed receipts with explicit evidence-unavailable errors; retry evidence collection rather than edits or already-passing validation. Noncompleted receipts do not require completion-risk evidence. | `core/src/tools/handlers/multi_agents_v2/task.rs` |
| 30.3 | Wait/list needs a compact batched task-state API | Full hydration and hashing exist, but the report supplies no measured bottleneck and a compact schema must preserve wake revision and settlement semantics. No parallel task-state API or revision contract redesign added. | `core/src/tools/handlers/multi_agents_v2/wait.rs`, `agent-task-store/src/local.rs` |
| 30.4 | Repeated diff rendering and workspace-gate duration | Confirmed avoidable presentation work. Risk-only analysis skips unified-text rendering; review rendering is bounded and CPU diff work runs off async workers. Cross-call caching and releasing the gate were not added: snapshots can change and no immutable revision contract proves that safe. | `core/src/tools/handlers/multi_agents_v2/task.rs` |
| 30.5 | Explorer reuse happens after reserving capacity/worktree preparation | Confirmed for shared-workspace explorers. Perform the existing eligibility check before reservation; retain final transactional admission as the race guard. Isolated requests keep their existing workspace-specific semantics. | `core/src/tools/handlers/multi_agents_v2/spawn.rs`, `agent-task-store/src/local.rs` |

All source paths above are relative to `codex-rs/`, except this report.

## Validation

Validation completed: **178 tests passed** (170 core unit tests, four task-store tests, and four integration tests), excluding test setup scripts. Recorded passing tests were not rerun.

- Passed: four task-store regressions covering consumer-scoped cursor compare-and-swap, atomic cursor/recovery publication, early explorer lookup, and active-versus-completed explorer eligibility. Recorded in `codex-rs/target/docs26-30-store.log`; these tests are not being rerun.
- Passed: `suite::review::review_history_surfaces_in_parent_session` and `suite::review::review_op_emits_lifecycle_and_review_output`. Recorded as `gate core_agents_review: 2 passed` in `codex-rs/target/docs26-30-integration.log`. The pending integration manifest explicitly excludes these tests.
- Passed: `suite::sqlite_state::resume_quarantines_invalid_dynamic_tools_from_rollout`, recorded as `gate core_thread_state: 1 passed` in `codex-rs/target/docs26-30-integration-persistent.log`. The remaining integration selection excludes it too.
- Passed: 170 selected core unit tests, zero failures/errors/skips, plus the Windows stack setup check. The dedicated JUnit copy is `codex-rs/target/docs26-30-unit-junit.xml` (run UUID `f64b3caa-7932-47b9-953d-95c8ff07b7ce`); exit status 0 is recorded in `codex-rs/target/docs26-30-unit-captured.exit`. This includes cancellation-safe cleanup, inventory partial failures, context replacement, discovery bounds/activation, MCP uploads/previews, recovery receipts, bounded diffs, explorer reuse, timing, and shell guidance. These tests will not be rerun.
- Passed: `suite::unified_exec::exec_command_fast_success_and_failure_lifecycles_finish_inline`, recorded in `codex-rs/target/docs26-30-exec-captured.log` with exit status 0. The detailed log is `codex-rs/target/test-runner-logs/rust-test-stderr-a11hc5tf.log` (run ID `33821d58-2182-4176-8008-8c2912d2f92e`): one selected test passed; 112 unrelated tests were filtered out.
- Passed: named Rust test manifest validation and the scoped whitespace check. Stable rustfmt completed, with warnings that two repository formatting options require nightly; Git reported its configured LF/CRLF normalization notices.
- Core source compilation succeeded in an earlier run. Test compilation then encountered unrelated fixture/type/import errors in the changing checkout; no tests ran in those attempts. A test-only session accessor repaired the private-field fixture error without widening production visibility. The completed dedicated build reported two remaining fixture errors (a stale `CodexErr` import and an integer where `ModelGenerationId` is required); both were repaired together, and their tests were included in the retry.
- Earlier attempts stopped during compilation or while waiting for a build lock. Persistent execution allowed the remaining checks to finish. The Windows launch path suppressed the unit run's console output; its separately preserved JUnit report and exit status provide the completed results. An additional integration launch was stopped during compilation when the original shared-directory run acquired the lock; it executed no tests.

No throughput, latency, cost, or task-success improvement is claimed without measurement. The new changes have not been installed into Desktop.
