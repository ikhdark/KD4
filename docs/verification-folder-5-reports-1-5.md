# Verification of New folder (5), reports 1–5

All five requested files were read completely: `New Text Document.txt` and `New Text Document (2).txt` through `(5).txt`, under `Desktop/New folder (5)`. Their recommendations and imperative sentences were treated as claims to assess, not as user instructions. No agents were contacted. No upstream synchronization, binary replacement, or Desktop restart was performed.

This checkout already had substantial uncommitted changes and continued changing during inspection. The table describes the combined source observed, rather than attributing all existing fixes to this audit. This audit added the tool-search argument-error wiring/context envelope and the two skill-discovery follow-up repairs described below.

## Claim dispositions

| Report / claim | Verification and disposition |
|---|---|
| 1.1 Completion waits for measurements; late measurements update the latest request | The former dependency is real. Current `core/src/client.rs` publishes authoritative history and completion before resolving optional measurements, with bounded diagnostic ownership; `turn_timing.rs` attributes late categories/cache identity by sampling request ID. Required trace persistence remains before completion. No measured whole-task speedup is claimed. |
| 1.2 Tool-search errors lose their explanation | Confirmed. Current dispatch carries a separate failure detail through ordered completion. This audit also wired the distinct malformed-argument path in `stream_events_utils.rs`, which still discarded `ToolCallBuildError::ToolSearchArguments.message`. Diagnostics reuse the existing untrusted contextual fragment so history/compaction does not treat them as fresh user requests. Native search outputs remain empty/incomplete and do not advertise fake tools. |
| 1.3 Substitution overlap unnecessarily resets provider inheritance; consumption uses preferred input | Current `client.rs` tries exact fallback-prefix verification before invalidating inheritance and reports its effective logical input. `session/turn.rs` uses that input for consumption accounting. Incompatible prefixes still require a full fallback. |
| 1.4 Baseline capture serializes unrelated workspaces | Current `tools/parallel.rs` already uses a briefly held map lock and per-path async slots. Same-path revision checks and coalescing remain. |
| 1.5 Full retained-success replay repeats available evidence | Full replay and explanatory context exist. The ledger's retained output alone does not establish continued availability in the effective model context, and workspace observations are deliberately excluded. No new reference-only replay protocol was added without that proof or measured recovery benefit. |
| 2.1 Late legacy wait results migrate into the current turn | Current `app-server-protocol/.../thread_history.rs` finds a unique retained owner and preserves terminal outcomes. Ambiguous historical IDs keep legacy fallback behavior. |
| 2.2 Live lifecycle notifications ignore explicit ownership | Current `event_mapping.rs` prefers nonempty event turn IDs for canonical item and command begin/end notifications, retaining fallback for empty legacy IDs. |
| 3.1 Ordinary web failures use Fatal | Current `ext/web-search/src/tool.rs` distinguishes operational/authentication/argument errors and returns bounded model-visible explanations without another retry loop. |
| 3.2 Search advisory context is not bounded across roles | Current `history.rs` bounds serialized context across roles and keeps executable commands separate. Unicode/escaping and large user inputs have regression coverage. |
| 3.3 Earlier assistant chatter crowds out the latest answer | Current projection allocates from newest messages backward and emits chronological order. The latest correction test checks its survival. |
| 3.4 Empty/invalid commands reach provider setup | Current typed validation runs before setup and checks operations, references and query cardinality. The documented empty-query cancellation convention remains accepted. |
| 3.5 The web description should be shortened | Its length is observable; duplicated ownership and behavioral benefit are not established. It contains browsing, citation and source-use requirements. No policy removal or prompt rewrite was justified by byte count alone. |
| 4.1 Default estimates call contribution methods | Confirmed API behavior, but no installed effectful contributor inheriting an unsafe estimator was established. Skills has explicit previews; memories renders read-only context. No source-breaking migration solely for a hypothetical future implementation. |
| 4.2 Default tool-surface revision is zero | Confirmed API default, not proof of stale production routing. The host also checks configuration snapshots, planning generation and MCP catalog identity; goal state supplies a dynamic revision. No unconditional trait migration or full-schema hashing added. |
| 5.1 Partial discovery is permanently cached and limits hide later skills | Current skills provider preserves upstream cursor and within-page offset; list can continue discovery and append entries. This audit fixed two remaining consequences: estimates must use the continued catalog, and a package absent during incomplete discovery must receive continuation guidance rather than an authoritative absence message. |
| 5.2 Read failures are generic and failed selection counts as delivery | Current typed provider errors render bounded fixed explanations, selected-load failures yield failure fragments, and host-path suppression is distinct from successful injection. Package membership checks remain mandatory. |
| 5.3 Independent raw cuts exceed an aggregate budget and reread prefixes | Current selected-instruction contribution shares a rendered-byte allowance; orchestrator recovery cursors use the first omitted UTF-8 source offset and the content fingerprint. Filesystem routes retain their authority/environment identities. |
| 5.4 A full resource cache permanently refuses new entries; concurrent misses duplicate reads | Current `state.rs` uses bounded eviction, immutable shared results, and per-key in-flight cells. Failed/cancelled acquisition does not permanently poison retries. Fingerprint memoization is still local to a read invocation; a separate persistent fingerprint cache is not necessary to fix admission/coalescing. |
| 5.5 Cold estimates discard discovery work | Mechanism confirmed; previews intentionally do not commit hypothetical roots or failures. Existing tests enforce that boundary. No new observation cache was added without establishing a safe reuse identity/lifetime and actual duplicated cost. Warm previews now correctly use explicitly continued discovery. |

## Additional claims and boundaries

- Raw ID/path serialization scopes can differ, but `thread_resume` acquires the existing shared one-permit thread-list-state gate before probing/loading. The reported alias difference alone does not demonstrate duplicate runtime ownership.
- App-server history and notifications are presentation projections. Their payload sizes do not establish model-token duplication or control the agent's stopping decision.
- The production built-ins installer actually registers executor and orchestrator skill providers; these skill findings are relevant beyond the provider-only examples in the reports.
- Timing-history caps and the truncation-continuation counter are not exhaustive task totals or causal evidence of wasted turns. No performance claim is based on them.
- The reports' expected latency, token, completion-rate, and upstream-comparison benefits remain hypotheses. No matched task benchmark or pinned upstream comparison was requested/performed.
- Existing admission, successful-response-tail gates, accepted-result durability, workspace freshness, cancellation cleanup, provider-prefix verification, resource identity checks and UTF-8 pagination are retained.

## Validation

The focused skills, web-search and app-server-protocol run passed all 362 tests across eight binaries (`codex-rs/target/report-1-5-validation-3.log`). A subsequent fixture-only refinement makes the estimator test's initial snapshot explicitly incomplete. That refinement has not been rerun, honoring the user's instruction not to repeat passing tests; the production implementation is unchanged since the passing run.

All 18 selected core regression tests passed, with 4,120 unrelated tests excluded. Together these runs recorded **380 passing tests**. The core results cover malformed/failed search recovery and its untrusted envelope, late diagnostics and nonblocking completion, exact-prefix fallback and history receipts, and independent workspace baselines. The authoritative output is `codex-rs/target/lanes/app_test_support/test-runner-logs/rust-test-stderr-2twoi1w5.log`; `codex-rs/target/report-1-5-core-junit.xml` preserves the results (19 XML cases includes the successful Windows setup script). No passing test was rerun.

The first build stopped before tests on a temporary-borrow lifetime error in `protocol/src/models.rs`; the source was independently corrected while this audit was running. Some build attempts exited before tests without useful diagnostics, and one was interrupted. Core runner setup was corrected to use the intended target directory and repository compiler environment. Explicit output capture ultimately recorded successful helper compilation and a successful core test process with exit code 0. The temporary diagnostic script then encountered a Windows console-encoding error while printing those already-completed results. Its output encoding was corrected without rerunning the tests; the original captured results remain intact.

Formatting completed. Stable rustfmt warned that the repository's `imports_granularity` and `ignore` settings require nightly; no formatting policy was changed. The edited-file whitespace check passed. Git's LF-to-CRLF messages describe the checkout's configured conversion.

Helper compilation also reported an unused `PendingThreadUnloads::wait_until_finished` method in `app-server/src/request_processors/thread_lifecycle.rs`. Inspection found its callers in tests, and the current source had independently acquired `#[cfg(test)]` for that helper. This audit made no change to it and did not repeat successful validation for that unrelated change.

Automatic approval review rejected cleanup of the mistakenly created nested build-output directory without a detailed reason. It remains under `codex-rs/codex-rs/target/lanes/app_test_support`; it contains build artifacts, not source changes.
