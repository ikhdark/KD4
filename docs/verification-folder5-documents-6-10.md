# Verification of folder (5), documents 6–10

All five supplied files were read completely. This report concerns `Desktop/New folder (5)/New Text Document (6).txt` through `(10).txt`; other similarly numbered reports in this checkout concern different attachments. Recommendations inside the documents were evaluated as claims, not executed as user instructions. No agents were contacted or created.

The baseline was the current, extensively modified checkout. Existing compatible fixes were preserved. Source evidence below is relative to `codex-rs/`. No upstream comparison, performance improvement, installed-binary activation, or Desktop restart is claimed.

Input SHA-256 identities (to distinguish similarly named attachments):

| Document | SHA-256 |
|---|---|
| 6 | `4AED4D38B6D877F5B96BFFE5A11D150222B9E14EA852813451BE632841ECC84B` |
| 7 | `33153C16D29DEE9788EE5A8F38C5D95A7830D8032304C06EEBBB8565B90C00AF` |
| 8 | `A8EB2E7EBD8923037562BB6BA35AE72072084A201E8C1BE674E4D8B43368FDC7` |
| 9 | `0A7083C6249B646E37C7B56117F0D6D3FA30DA9FC5683336EF490E34E69639D7` |
| 10 | `9490314CBC5BFD98C6A04B33071FF9DE4D3AEACCE3D9E12F7ACAA87C4BDA5551` |

## Dispositions

| Claim | Verification and action |
|---|---|
| 6.1 Invalid image data can be reported as success | Confirmed and fixed in `ext/image-generation/src/tool.rs`. Provider output is base64-decoded and fully PNG-decoded before completion, independently of saving. The existing image helper supplies decoded-allocation limits; encoded/source bytes use its existing source limit. Invalid data produces a failed lifecycle item and recoverable tool error. Saving reuses validated bytes. |
| 6.2 Failed/disabled saving is indistinguishable; host paths can be mistaken for workspace paths | Confirmed and fixed using the existing output-hint path consumed by both direct output and `generatedImage()`. Saved paths are identified as host-local; disabled saving and write failure have distinct explanations. Valid generated images remain returned after save failure and can be edited using existing history selection. A new durable artifact registry/export API is a separate proposed feature, not necessary to expose these outcomes. Failed saving does not promise restart durability. |
| 6.3 Recent-image windows cannot select arbitrary nonadjacent/older images | Confirmed interface limitation in `request_for_call_args`/`recent_images`, already stated in the tool description. Exact filesystem paths cover addressable assets; pathless images have the documented bounded fallback. Replacing the host history contract with a persistent image registry is a redesign, not a demonstrated violation of the current selector contract. No partial registry or new reference protocol added. |
| 6.4 Image strings are copied into canonical and legacy lifecycle events | Confirmed in `legacy_end_event` and `extension_turn_item`. Both external representations have consumers. Shared blob ownership/wire redesign and net allocation savings were not established by a workload; compatibility preserved. These byte copies do not establish repeated model-token charges. |
| 6.5 Reference preparation is serial, synchronous, and unbounded at its read boundary | Confirmed missing read bound and executor-blocking decode; fixed by using `read_file_bounded` with the existing source-byte limit and `spawn_blocking` for decoding. Existing decoder limits are reused. Processing remains serial and ordered, limiting simultaneous buffers; speculative concurrent preparation was not added without a measured benefit. |
| 7.1 RPC classification rejects malformed requests instead of interpreting them as notifications | Confirmed in `app-server-protocol/src/rpc.rs`; preserved. Reserved-field classification and existing malformed/extension-field tests support the claim. |
| 7.2 All overload constructors say retryable | Confirmed constructor behavior. Examined `in_process.rs`, `message_processor.rs`, and client forwarding: request queue rejection occurs before dispatch, while server-request delivery failures are returned to their owner. No automatic replay of a completed side effect was established. No global retry-policy change. |
| 7.3 Experimental checks/exporter overhead is not established agent-loop overhead | Confirmed short-circuit `find_map` implementations in `experimental_api.rs`; schema tooling remains separate from model prompt construction. No optimization or token claim justified. |
| 8.1 Interrupt target is discarded between validation and execution | Confirmed and fixed. App-server calls existing `CodexThread::interrupt_turn_if_active`, whose terminal scheduler compares identity under the active-turn lock. Pending acknowledgements now include the target ID, and either the matching terminal event or completed core operation claims the response once. Startup cancellation retains its existing unqualified operation. The separate claim that natural completion never acknowledges interrupts was already false: `bespoke_event_handling.rs` handles both normal and aborted completion. |
| 8.2 Render estimate does not limit retained additional-context bytes | Confirmed and fixed. Before rendering estimates/fingerprints/submission, `map_additional_context` sums complete UTF-8 key/value byte lengths with saturating arithmetic and enforces 128 KiB, reporting reason, actual bytes, and maximum bytes. Both start and steer use this mapper. The transport allocation is a separate boundary. |
| 8.3 Default task fingerprint adds serialization without cross-thread deduplication | Confirmed mechanism in `turn_task_fingerprint`, but removal changes the documented duplicate-error contract. Also, `ThreadStateManager` currently ties fingerprint capacity to the content index; blindly passing `None` would bypass that limit. No measured admission bottleneck or incorrect admitted task establishes a need to delete this mechanism in this correctness pass. Preserved rather than silently changing concurrency/error semantics. |
| 8.4 Goal-resume continuation waits behind unbounded snapshot delivery | Confirmed and fixed. Snapshot delivery uses the existing resource-delivery deadline, then invokes core's idle check. The resume response, usage notification, snapshot, and idle handoff have one background-task owner so caller cancellation cannot strand finalization after response construction. Healthy-path ordering is preserved; a timed-out snapshot is recoverable via existing `thread/goal/get`. Shutdown status suppresses continuation. |
| 8.5 Steering error conversion discards typed recovery details | Confirmed and fixed. Existing messages/codes/analytics remain; no-active-turn, mismatched IDs, empty input, and pending limits have structured data. Existing review/compact error data remains intact. No automatic retargeting or unconditional retryability was added. |
| 9.1 Retry reuses sealed workspace evidence | Partly already fixed for attempts accepting completed output, but still reproducible from source for an established stream that ends before any `OutputItemDone`: cleanup seals the batch while retry skipped rebuilding it. Fixed that branch with a fresh batch on a cloned capability snapshot, and rebuilt direct/code-mode runtimes for every retry. Old owners retain their old batch; no unsealing. |
| 9.2 Direct validation misses lease-protected revision attribution | Already fixed in `tools/parallel.rs`: successful executed payload attribution is outside nested evidence-persistence classification and happens before releasing the lease. Existing direct/nested regression selected for verification. |
| 9.3 Baseline capture holds a global lock over I/O | Already fixed: a short map lock selects an `Arc<tokio::sync::Mutex<...>>` per workspace, then capture holds only that workspace's lock. Existing independence regression selected for verification. |
| 9.4 Native discovery failure loses the cause | Already fixed by bounded failure-detail projection in `parallel.rs`, ordered insertion in `session/turn.rs`, and parse-failure handling in `stream_events_utils.rs`. Native search remains incomplete with no fabricated tools. |
| 9.5 Retained replay appends a developer notice and full result | Confirmed extra history write. The notice explicitly distinguishes retained evidence from a fresh observation, and replay excludes workspace observations. Suppressing bodies requires final-request availability proof; removing provenance without a replacement would weaken semantics. A result-provenance redesign is not a verified correctness fix, and no wire-token or latency benefit was measured. Preserved. |
| 10.1 Outbound user-input conversion can panic on unsupported variants | The wildcard panic exists, but all six current variants in `protocol/src/user_input.rs` are handled in `app-server-protocol/src/protocol/v2/turn.rs`. No currently reachable unsupported variant exists. A future-proof fallible projection is a design proposal; no runtime defect reproduced, so no broad conversion migration. |
| 10.2 Projected MCP result drops `is_error` | Confirmed representation difference. `core/src/mcp_tool_call.rs` translates tool-reported errors into failed item status; the app-server preserves status/content, and direct MCP RPC preserves the optional flag. No consumer requiring the extra nested field was established. No schema expansion. |
| 10: Duplicated collaboration text, full-plus-paged resume, duplicate terminal fields | Representations exist. Resume has the explicit `excludeTurns` option. Terminal notification producer copies timing/surfaced result from its nested turn, keeping both representations consistent. No demonstrated duplicate model context or failing compatibility consumer supports removing fields. |
| Claims of broad speed, cost, completion-rate, or upstream superiority | Unestablished. Reports contain predictions and incomplete source snapshots, not paired workload measurements. No such improvement is claimed here. |

## Validation

**28 tests passed: 4 core, 8 app-server, and 16 image-generation tests. No passing test was rerun.** Test scope was limited to the affected behaviors and the image extension's small library suite. Formatting completed; stable rustfmt reported that the repository's nightly-only import/ignore settings are unavailable.

The first image test command exited unsuccessfully before executing tests and supplied no compiler/test diagnostic after waiting for the shared Cargo build lock. It is not counted as a test pass or a demonstrated code failure.

The first core regression build stopped at an unrelated `ModelAttemptOffsets` fixture missing `completed_us` in `core/src/client_tests.rs`. The fixture already contained the correction when inspected; this verification did not edit it. No selected core tests executed in that attempt. Interrupted image/app-server attempts likewise produced only build-lock waits. Only unexecuted checks were submitted on continuation; passing tests are not rerun.

Core regression result: **4 passed, 0 failed** (4,134 unrelated tests skipped), recorded in `codex-rs/target/test-runner-logs/rust-test-stderr-ib_ewp5e.log`. These cover fresh retry evidence, targeted interrupt/successor preservation, direct/nested validation attribution, and independent workspace baselines. These passing tests were not rerun.

Direct image validation exposed three compile errors in the new fixtures: redundant `.unwrap()` calls after `AbsolutePathBuf::join`. All three were corrected together after the build finished. No image test had executed before this repair.

App-server regression result: **8 passed, 0 failed** (515 unrelated tests filtered out), recorded in `codex-rs/target/test-runner-logs/folder5-docs6-10-app-binary.log`. The queued Cargo commands were repeatedly terminated while waiting on the shared build lock. The app-server tests were therefore run once by exact name from the freshly compiled `codex_app_server-25014ca27864fa1c.exe` (built after these source changes), with `INSTA_UPDATE=no` and the repository's 8 MiB test-stack setting. Coverage includes raw/rendered context budgets, steering recovery fields, interrupt acknowledgement/cancellation, and healthy/backpressured goal finalization.

Image-generation result: **16 passed, 0 failed**, recorded in `codex-rs/target/test-runner-logs/folder5-docs6-10-image-final.log` (nextest run `ff23b5d1-a483-42e7-b0f8-208e05e19718`, exit 0). This includes invalid-provider-data rejection, actual completion lifecycle across all save configurations, disabled/successful/failed persistence, bounded reference reads, selector behavior, direct output, and code-mode output hints. The final run used the local profile with retries explicitly disabled.

All selected checks finished successfully. No installed binary was replaced and Desktop was not restarted.
