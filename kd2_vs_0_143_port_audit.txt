KD2 vs codex-rust-v0.143.0 port audit

Base tree:
  C:\Users\kuh\Desktop\codex-rust-v0.143.0\codex-rs

KD2 tree:
  C:\Users\kuh\Desktop\codexKD2\codex-rs

Report location:
  C:\Users\kuh\Desktop\kd4\kd2_vs_0_143_port_audit.txt

Purpose:
  Identify KD2 changes that are worth porting into the newer upstream-based fork,
  while avoiding older KD2 rollbacks that would overwrite better v0.143.0 code.

Follow-up status on KD4 main, 2026-07-09
---------------------------------------
- PORT #1, Local runtime provenance, local watermark, and publish/build doctor:
  covered by current KD4. The protocol-visible initialize metadata and server
  info structs are already present, and KD4 has fork-local improvements beyond
  KD2: default LOCAL-KD publish target discovery, pnpm install-method labeling,
  newer doctor auth handling, remote MCP stdio checks, and current WebSocket
  reachability wiring. Do not port KD2's older doctor/runtime files over current
  KD4.

  Confirmed current KD4 paths:
    codex-rs/build_info.rs
    codex-rs/app-server/src/build_info.rs
    codex-rs/app-server/src/runtime_provenance.rs
    codex-rs/app-server/src/local_watermark.rs
    codex-rs/app-server/src/request_processors/initialize_processor.rs
    codex-rs/app-server-protocol/src/protocol/server_info.rs
    codex-rs/app-server-protocol/src/protocol/v1.rs
    codex-rs/cli/src/build_info.rs
    codex-rs/cli/src/doctor.rs
    codex-rs/cli/src/doctor/runtime.rs
    codex-rs/cli/src/doctor/git.rs
    codex-rs/cli/src/doctor/output/detail.rs

  Inspection result:
    app-server-protocol/src/protocol/server_info.rs is equivalent to KD2
    app-server-protocol/src/protocol/v1.rs is equivalent to KD2 for the
      buildInfo/runtimeInfo/localWatermark initialize response fields
    KD4 runtime_provenance/local_watermark/doctor files should be kept because
      their differences are current-fork improvements rather than regressions

- PORT #2, Enterprise/custom CA HTTP and WebSocket transport:
  covered by current KD4. KD4 has the KD2 custom CA behavior in the newer
  codex-rs/http-client crate, plus newer WebSocket wiring through
  codex-rs/websocket-client. Do not port KD2's older codex-client/codex-api
  transport files over current KD4. This pass also routed local MCP OAuth
  discovery/status call sites through the shared ReqwestHttpClient so custom CA
  handling reaches the default MCP OAuth support path.

  Confirmed current KD4 paths:
    codex-rs/http-client/src/custom_ca.rs
    codex-rs/http-client/src/lib.rs
    codex-rs/http-client/tests/ca_env.rs
    codex-rs/http-client/src/bin/custom_ca_probe.rs
    codex-rs/websocket-client/src/lib.rs
    codex-rs/websocket-client/src/dialer.rs
    codex-rs/codex-api/src/endpoint/responses_websocket.rs
    codex-rs/codex-api/src/endpoint/realtime_websocket/methods.rs
    codex-rs/backend-client/src/client.rs
    codex-rs/codex-api/src/files.rs
    codex-rs/cloud-tasks/src/env_detect.rs
    codex-rs/exec-server/src/client/reqwest_http_client.rs
    codex-rs/login/src/auth/default_client.rs
    codex-rs/codex-mcp/src/mcp/auth.rs
    codex-rs/cli/src/mcp_cmd.rs
    codex-rs/app-server/src/request_processors/plugins.rs
    codex-rs/core/src/mcp_skill_dependencies.rs

  Validation run:
    cargo test -p codex-http-client --test ca_env
    cargo test -p codex-websocket-client
    cargo check -p codex-websocket-client -p codex-api -p codex-backend-client -p codex-exec-server -p codex-mcp -p codex-cli -p codex-app-server -p codex-core
    cargo check -p codex-mcp -p codex-cli -p codex-app-server -p codex-core
    cargo test -p codex-mcp

- PORT #3, Source discovery, source evidence, and path-aware search tools:
  confirmed still missing from current KD4. KD2 has real code here, but it is
  not a narrow file copy. The KD2 source tools depend on KD2-only support modules
  and protocol/config pieces:
    codex-rs/core/src/tools/handlers/source.rs
    codex-rs/core/src/tools/handlers/source_spec.rs
    codex-rs/core/src/source_evidence.rs
    codex-rs/core/src/path_aware_search.rs
    codex-rs/core/src/discovery_ledger.rs
    codex-rs/core/src/turn_route.rs
    codex-rs/core/src/workspace_index.rs
    codex-rs/core/src/repo_guidance_evidence.rs
    codex-rs/core/src/context/repo_guidance_evidence.rs
    codex-rs/core/tests/suite/repo_guidance_evidence.rs
    codex-rs/file-search/src/source_search.rs
    codex-rs/file-search/src/source_routes.rs
    codex-rs/file-search/src/source_search_main.rs
    codex-rs/features/src/lib.rs
    codex-rs/protocol/src/source_citation.rs
    codex-rs/config/src/coding_budgets.rs

  Current KD4 lacks the source-tool modules, protocol citation type, source
  budget constants, source-search file-search binary/module, and the KD2
  SourceTools feature gate. Stage this separately as a model-visible runtime
  change: first port or recreate the supporting protocol/types and bounded
  handler contracts, then wire tool registration, then validate registration,
  path normalization, bounded reads/searches, ranking, output shape, and
  code-mode/tool-search visibility.

- PORT #4, verify_local validation tool and local verifier routing:
  first-stage core tool port is now in this working tree. The repo-local script
  layer was already here, and KD4's script version is newer than KD2's because
  it adds PowerShell and shell script syntax validation. Keep KD4's script layer;
  do not overwrite it with KD2.

  Confirmed current KD4 paths:
    scripts/verify_local.py
    scripts/verify_local_rules.toml
    scripts/test_verify_local.py
    codex-rs/core/src/tools/handlers/verify_local.rs
    codex-rs/core/src/tools/handlers/verify_local_spec.rs
    codex-rs/core/src/tools/handlers/verify_local_tests.rs
    codex-rs/core/src/tools/handlers/verify_local_spec_tests.rs
    codex-rs/core/src/tools/handlers/mod.rs
    codex-rs/core/src/tools/spec_plan.rs
    codex-rs/core/src/tools/spec_plan_tests.rs

  Added in this pass:
    per-turn proof-bearing freshness recording in TurnDiffTracker from
    verify_local VERIFIED results, successful validation command completion,
    failed/timed-out validation command completion, broad-filter advisory
    commands, formatting-only commands, and later write/mutation evidence.
    Final turn completion now emits a warning when changed files do not have
    fresh post-change validation evidence, and invalidated diff tracking now
    falls back to git diff for a best-effort turn diff.

  Still not ported from KD2:
    KD2's full session_memory.rs cross-turn/compaction memento and richer
    verification capsule are not present in current KD4. The current fix covers
    the runtime freshness gap for this turn's tracker without introducing a new
    model-visible memory subsystem. Treat the full session-memory system as a
    separate broader stage if still wanted.

  Validation run:
    python -m unittest scripts.test_verify_local
    cargo test -p codex-core verify_local --lib
    cargo test -p codex-core turn_diff_tracker --lib
    cargo test -p codex-core unified_exec --lib
    cargo test -p codex-core command_shape --lib
    cargo test -p codex-core command_preflight --lib
    cargo test -p codex-core shell_command --lib
    cargo test -p codex-core guardian_tests --lib
    cargo test -p codex-core shell_spec --lib
    cargo test -p codex-core exec_approval_requirement_uses_command_for_safety_for_policy --lib
    cargo test -p codex-protocol --lib
    cargo check -p codex-core
    just verify-local --plan --changed=codex-rs/core/src/tools/handlers/verify_local.rs --json
    git diff --check
    wiring_guard.py --session-id kd4-port-audit-fix-1-5 check --ledger --ledger-mode summary --intent-only

  Next safe stage:
    If full KD2 validation memory is still wanted, port or recreate the
    session-memory and compaction-visible verification capsule pieces as a
    separate model-visible stage. Do not mix that with command-preflight or
    source-tool ports.

- PORT #5, Command preflight diagnostics, command shape, and PowerShell guidance:
  first-stage core port is now in this working tree. The PowerShell wrapper
  scripts already matched KD2, and current codex-rs/shell-command already had
  PowerShell parsing/safety tests. This stage adds KD2's model-visible command
  shape fields and command preflight layer to KD4's current unified-exec path
  instead of replacing KD4's newer environment, sandbox, and process handling.

  Confirmed current KD4 paths that should be kept:
    scripts/run-powershell-script.ps1
    scripts/test_run_powershell_script.py
    codex-rs/shell-command/src/powershell.rs
    codex-rs/shell-command/src/command_safety/powershell_parser.rs

  Ported into current KD4:
    codex-rs/core/src/tools/handlers/command_shape.rs
    codex-rs/core/src/tools/handlers/command_shape_tests.rs
    codex-rs/core/src/tools/handlers/command_preflight.rs
    codex-rs/core/src/tools/handlers/command_preflight_tests.rs
    codex-rs/core/src/tools/handlers/shell.rs
    codex-rs/core/src/tools/handlers/shell/shell_command.rs
    codex-rs/core/src/tools/handlers/unified_exec.rs
    codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs
    codex-rs/core/src/tools/handlers/unified_exec_tests.rs
    codex-rs/core/src/tools/handlers/shell_spec.rs
    codex-rs/core/src/tools/handlers/shell_spec_tests.rs
    codex-rs/core/src/tools/handlers/shell_tests.rs
    codex-rs/core/src/tools/events.rs
    codex-rs/core/src/exec_policy.rs
    codex-rs/core/src/exec_policy_tests.rs
    codex-rs/core/src/unified_exec/mod.rs
    codex-rs/core/src/unified_exec/async_watcher.rs
    codex-rs/core/src/unified_exec/process_manager.rs
    codex-rs/core/src/tools/runtimes/unified_exec.rs
    codex-rs/protocol/src/models.rs

  Validation run:
    cargo test -p codex-core command_shape --lib
    cargo test -p codex-core command_preflight --lib
    cargo test -p codex-core unified_exec --lib
    cargo test -p codex-core shell_command --lib
    cargo test -p codex-core guardian_tests --lib
    cargo test -p codex-core shell_spec --lib
    cargo test -p codex-core exec_approval_requirement_uses_command_for_safety_for_policy --lib
    cargo test -p codex-core verify_local --lib
    cargo test -p codex-core turn_diff_tracker --lib
    cargo test -p codex-protocol --lib
    cargo check -p codex-core
    git diff --check
    wiring_guard.py --session-id kd4-port-audit-fix-1-5 check --ledger --ledger-mode summary --intent-only

  Remaining caveats:
    KD2's PowerShell post-failure advisory is covered by command_shape tests but
    is not production-wired because current KD4 ExecCommandToolOutput has no
    advisory field. Keep that as a separate output-contract decision if wanted.
    The legacy shell_command path is now on the same command-shape/preflight
    track as unified exec, including display-command proof for hooks and
    validation freshness. No other missing command-shape or preflight core code
    was found in the local recheck; the current caveat is the explicit
    output-contract choice above.

  Next safe stage:
    After final whitespace/wiring checks, move to PORT #3 source tools if that
    large model-visible runtime feature is still wanted.

Harness recheck on KD4 main, 2026-07-09
---------------------------------------
- Used the KD4 harness workflow as the check gate for PORT #1-#5.
- PORT #1 runtime provenance/local watermark/doctor: focused initialize
  metadata test passed; listed implementation paths still exist.
- PORT #2 custom CA HTTP/WebSocket: custom CA tests, websocket-client tests, and
  cross-crate call-site compile check passed.
- PORT #3 source tools: still not implemented. Exact listed source-tool files
  remain absent, and no half-wired source-tool registration was found. This is
  still a separate broad model-visible stage.
- PORT #4 verify_local/freshness and PORT #5 command shape/preflight: focused
  core/protocol/script tests passed, and Wiring Guard reported WIRED for the
  declared runtime edges.
- Cleanup during this recheck: removed an unused ToolEmitter::unified_exec
  wrapper after cargo check showed the display-command path had replaced all
  call sites.

  Validation run:
    cargo test -p codex-app-server initialize_response_includes_local_runtime_metadata
    cargo test -p codex-http-client --test ca_env
    cargo test -p codex-websocket-client
    cargo check -p codex-websocket-client -p codex-api -p codex-backend-client -p codex-exec-server -p codex-mcp -p codex-cli -p codex-app-server -p codex-core
    cargo test -p codex-core command_shape --lib
    cargo test -p codex-core command_preflight --lib
    cargo test -p codex-core shell_command --lib
    cargo test -p codex-core shell_spec --lib
    cargo test -p codex-core unified_exec --lib
    cargo test -p codex-core verify_local --lib
    cargo test -p codex-core turn_diff_tracker --lib
    cargo test -p codex-core guardian_tests --lib
    cargo test -p codex-core exec_approval_requirement_uses_command_for_safety_for_policy --lib
    cargo test -p codex-protocol --lib
    cargo check -p codex-core
    python -m unittest scripts.test_verify_local
    just verify-local --plan --changed=codex-rs/core/src/tools/handlers/verify_local.rs --json
    wiring_guard.py --session-id kd4-port-audit-fix-1-5 check --ledger --ledger-mode summary --intent-only
    git diff --check

  Subagent result:
    PORT #3 review returned no audit fix and confirmed the feature is still
    absent. PORT #1/#2 and PORT #4/#5 subagents did not return before timeout
    and were interrupted; root validation above is the completion evidence.

Harness implementation status for PORT #5-#15 on KD4 main, 2026-07-09
---------------------------------------------------------------------
- Used the KD4 harness workflow for this pass. The root AGENTS policy,
  `.codex/AGENTS.md`, `.codex/harness/workflow.md`, and the skill references
  `.codex/skills/kd4-harness/references/quality-gates.md` and
  `.codex/skills/kd4-harness/references/task-artifacts.md` were the active
  finish and artifact gates.

- PORT #5, command preflight/shape/output:
  implemented and rechecked. The remaining KD2 gap from the earlier note is now
  closed: PowerShell script-body failure advisories are wired into
  ExecCommandToolOutput and the one-shot unified-exec completion path. The
  preflight layer also keeps KD4's newer command safety path while adding
  stronger Windows/cmd/PowerShell segmentation and shell-output summary tests.

- PORT #6, config explain, requirements, MCP requirements, and coding budgets:
  implemented/present and rechecked. Current KD4 has the config requirements
  stack, MCP requirements, config explain, and coding budget modules wired from
  `codex-rs/config/src/lib.rs`. This pass aligned memory budget constants to
  the shared coding budget constants and validated both config requirements and
  schema drift.

- PORT #7, session/evidence memory:
  not completed in this pass. A subagent feasibility check found this is not a
  narrow transplant: KD2's session memory depends on missing source tools,
  `protocol::source_citation`, source evidence/discovery ledger plumbing,
  execution receipts, dirty-worktree baselines, and older session context paths.
  Current KD4 also has newer WorldState/StepContext and turn diff attribution
  paths that would need an integration design. Treat #7 as a separate blocked
  model-visible stage, not as implemented.

- PORT #8, app-server startup config fallback:
  implemented/present and rechecked. Non-strict startup now installs managed
  config/cloud/thread loaders from the effective fallback/default config instead
  of leaving bootstrap no-op loaders active after preload failure.

- PORT #9, structured turn-start validation:
  implemented/present and rechecked. V2 text-element byte ranges are validated
  before turn start, image URL validation is still preserved, and invalid
  environment/thread-settings paths now return stable error data.

- PORT #10, remote-control enrollment and refresh lifecycle:
  current KD4 is already at least as good as KD2 for this area. The remote
  control server_api/websocket/enrollment lifecycle and refresh tests are
  present and passed the focused validation.

- PORT #11, state and persistence indexes:
  implemented/present and rechecked. The job/thread, feedback-log, and memory
  selection index migrations are present under `codex-rs/state`.

- PORT #12, rollout read-repair and search snippets:
  implemented/present and rechecked. Rollout read-repair loads full rollout
  items before reconciliation, rollout search discovers current rollout files,
  parses ripgrep JSON snippets, and still scans compressed rollouts.

- PORT #13, generated memory-file writes:
  implemented/present and rechecked. Generated memory artifacts use temp-file
  plus rename and skip byte-for-byte unchanged writes.

- PORT #14, TUI `/diff` untracked cap:
  implemented/present and rechecked. `/diff` caps untracked file diffs and
  reports omitted paths.

- PORT #15, `/ideas` slash command:
  implemented/present and rechecked. The prompt asset, slash command, dispatch,
  and exact-match popup selection are covered.

  Validation run for this #5-#15 harness pass:
    cargo test -p codex-shell-command parse_command --lib
    cargo test -p codex-core command_preflight --lib
    cargo test -p codex-core command_shape --lib
    cargo test -p codex-core shell_output_summary --lib
    cargo test -p codex-core exec_command_tool_output --lib
    cargo test -p codex-core unified_exec --lib
    cargo test -p codex-config config_explain --lib
    cargo test -p codex-cli config_explain
    cargo test -p codex-config requirements --lib
    just config-schema-check
    cargo test -p codex-app-server --test all initialize_reports_non_strict_config_preload_failure
    cargo test -p codex-protocol user_input --lib
    cargo test -p codex-app-server --test all turn_start_rejects_invalid_text_element_range_before_starting_turn
    cargo test -p codex-app-server --test all turn_start_rejects_invalid_permission_selection_before_starting_turn
    cargo test -p codex-app-server --test all turn_start_rejects_unknown_environment_before_starting_turn
    just app-server-schema-check
    cargo test -p codex-app-server-transport remote_control --lib
    cargo test -p codex-state migration --lib
    cargo test -p codex-rollout search --lib
    cargo test -p codex-memories-write generated_file --lib
    cargo test -p codex-tui get_git_diff_caps_untracked_file_diffs_and_lists_omitted_paths --lib
    cargo test -p codex-tui selecting_ideas_by_exact_match --lib
    git diff --check

  Validation caveats:
    cargo test -p codex-app-server turn_start --lib still stack-overflows in
    message_processor::message_processor_tracing_tests::turn_start_jsonrpc_span_parents_core_turn_spans.
    The focused v2 turn_start integration tests above passed.

    Wiring Guard proved the representative must-reach symbols for #5, #8, #9,
    #10, #12, #13, #14, and #15, but the broad verdict stayed INCONCLUSIVE
    because the scan budget was exceeded in this large dirty working tree.

  Desktop visibility:
    Source changes are not installed into the desktop app. Desktop-visible proof
    still requires `just publish-local-codex-final` and a Codex Desktop restart.

Method and coverage
-------------------
- Compared both complete codex-rs trees, excluding .git, target, and vendor.
- Built raw and semantic inventories. The semantic pass normalized CRLF/LF and
  used the same judgment basis as `git diff --no-index --ignore-cr-at-eol`.
- Used KD2's own post-import local history as a second lens:
  C:\Users\kuh\Desktop\codexKD2, commit range 69fdf8d..9afd23d.
- Spawned three subagents as requested:
  - runtime/core/config/tools/shell/apply-patch/protocol
  - app-server/protocol/state/thread/session/persistence/MCP/remote-control
  - UI/CLI/plugins/models/memory/prompts/utils/client transports
- Spawned three additional subagents for the #1-#5 implementation recheck on
  2026-07-09. They did not deliver actionable findings before being interrupted;
  the final fixes and validation above came from the local deterministic recheck.
- Treated generated schemas, generated TypeScript, snapshots, lockfiles, and
  broad delete-heavy rollbacks as evidence, not as direct port candidates.
- KD4 had a pre-existing tracked dirty file before this audit:
  codex-rs/Cargo.lock. This audit did not touch it.

Inventory totals
----------------
Semantic inventory, all files:
  added_in_kd2:   786
  modified:      1491
  removed_in_kd2: 277
  same:          3108

Semantic inventory after excluding README/AGENTS/SOURCE_MAP, schema output,
and snapshots:
  added_in_kd2:   156
  modified:      1262
  removed_in_kd2: 227

Raw inventory artifacts:
  C:\Users\kuh\Desktop\kd4\kd2_vs_0_143_diff_inventory.tsv
  C:\Users\kuh\Desktop\kd4\kd2_vs_0_143_semantic_inventory.tsv

Plain text full inventory:
  C:\Users\kuh\Desktop\kd4\kd2_vs_0_143_full_file_inventory.txt

Decision labels
---------------
PORT:
  Worth bringing forward, but still needs normal KD4 call-path validation.

MAYBE:
  Useful idea or partial improvement, but current v0.143.0 may already have
  newer behavior, or the change needs deeper current-fork reconciliation.

SKIP:
  Do not port wholesale. Usually generated drift, an older rollback, or already
  present/superseded upstream behavior.

PORT first
----------

1. Local runtime provenance, local watermark, and publish/build doctor

Paths and pieces:
  build_info.rs
  app-server/build.rs
  app-server/src/build_info.rs
  app-server/src/runtime_provenance.rs
  app-server/src/local_watermark.rs
  app-server/src/request_processors/initialize_processor.rs
  app-server-protocol/src/protocol/server_info.rs
  app-server-protocol/src/protocol/v1.rs
  cli/src/build_info.rs
  cli/src/doctor/runtime.rs
  cli/src/doctor/git.rs
  cli/src/doctor/output/detail.rs
  cli/src/doctor.rs
  cli/tests/version.rs

Why KD2 is better:
  This is directly aligned with KD4's local Desktop workflow. KD2 reports the
  running executable, build commit/dirty/profile/timestamp, local package layout,
  expected local publish target, binary hash/version match, and warnings with
  concrete remedies such as publish-local-codex-final and Desktop restart.

Port caveats:
  This changes the app-server initialize contract. Regenerate protocol schemas
  from source after porting. Keep it fork-local and Windows/local-build aware.
  Validate through app-server initialize metadata, local binary path/hash, and
  Desktop-visible runtime proof if this is made visible to Desktop.

2. Enterprise/custom CA HTTP and WebSocket transport

Paths and pieces:
  codex-client/src/custom_ca.rs
  codex-client/src/default_client.rs
  codex-client/src/request.rs
  codex-client/src/transport.rs
  codex-client/src/chatgpt_cloudflare_cookies.rs
  codex-client/src/chatgpt_hosts.rs
  codex-client/src/error.rs
  codex-client/tests/ca_env.rs
  codex-client/src/bin/custom_ca_probe.rs
  codex-api/src/endpoint/websocket.rs
  backend-client/src/client.rs
  rmcp-client/src/rmcp_client.rs

Why KD2 is better:
  Adds CODEX_CA_CERTIFICATE / SSL_CERT_FILE support, rustls and reqwest builder
  configuration, PEM bundle handling, subprocess-isolated tests, and WebSocket
  TLS connector wiring. This is one of the cleanest high-value KD2 changes.

Port caveats:
  Cherry-pick the transport abstraction and call-site wiring only. Do not port
  whole codex-api, backend-client, or rmcp-client diffs because KD2 also removes
  newer v0.143.0 safety, rate-limit, auth, and startup behavior there.

3. Source discovery, source evidence, and path-aware search tools

Paths and pieces:
  core/src/tools/handlers/source.rs
  core/src/tools/handlers/source_spec.rs
  core/src/source_evidence.rs
  core/src/path_aware_search.rs
  core/src/workspace_index.rs
  core/src/repo_guidance_evidence.rs
  core/src/context/repo_guidance_evidence.rs
  core/tests/suite/repo_guidance_evidence.rs
  file-search/src/source_search.rs
  file-search/src/source_routes.rs
  file-search/src/source_search_main.rs

Why KD2 is better:
  Adds search_source/read_file_span style source tools, owner/surface discovery,
  ranked source results, evidence coverage, and next-read hints. This improves
  local agent behavior for large repositories and reduces guesswork.

Port caveats:
  This is a broad model-visible runtime change. Stage it separately from
  verification and memory changes. Validate tool registration, bounds, ranking,
  path normalization, performance, and output shape.

4. verify_local validation tool and local verifier routing

Paths and pieces:
  core/src/tools/handlers/verify_local.rs
  core/src/tools/handlers/verify_local_spec.rs
  core/src/tools/handlers/verify_local_spec_tests.rs
  core/src/tools/handlers/verify_local_tests.rs
  core/src/tools/spec_plan.rs
  core/src/tools/spec_plan_tests.rs
  core/src/session_memory.rs
  KD2 root scripts/verify_local.py
  KD2 root scripts/verify_local_rules.toml

Current port status:
  Core tool routing and first-turn freshness handling are now ported in this
  working tree. The verify_local model tool is registered only when a selected
  local environment is inside a repo with scripts/verify_local.py and justfile
  markers. It runs current KD4's just verify-local wrapper, rejects broadening
  or mutating verifier arguments, parses verifier verdicts, records proof-
  bearing VERIFIED evidence into TurnDiffTracker, and keeps KD4's newer script
  layer.

Remaining KD2 advantage:
  KD2 also has a full cross-turn session_memory.rs verification capsule and
  richer compaction-visible memento. That subsystem is not present in current
  KD4 and was intentionally not ported in this patch.

Port caveats:
  Keep the full session-memory piece separate unless current KD4 restores that
  subsystem. Do not overwrite KD4's scripts/verify_local.py or
  scripts/verify_local_rules.toml with KD2 because KD4's script layer has newer
  local shell validation behavior.

5. Command preflight diagnostics, command shape, and PowerShell guidance

Paths and pieces:
  core/src/tools/handlers/command_preflight.rs
  core/src/tools/handlers/command_preflight_tests.rs
  core/src/tools/handlers/command_shape.rs
  core/src/tools/handlers/command_shape_tests.rs
  core/src/tools/handlers/shell.rs
  core/src/tools/handlers/shell/shell_command.rs
  core/src/tools/shell_output_summary.rs
  shell-command/src/command_safety/powershell_parser.ps1
  shell-command/src/command_safety/powershell_parser.rs
  shell-command/src/command_safety/windows_safe_commands.rs
  shell-command/src/parse_command.rs
  shell-command/src/powershell.rs
  shell-command/src/rg.rs

Why KD2 is better:
  Better detection for shell mismatch, malformed quoting, direct argv PowerShell
  cmdlet mistakes, unsafe rg options, glob/path mistakes on Windows, and common
  command typos. KD2 also has better failure guidance for PowerShell script-body
  retries and bounded shell output summaries.

Port caveats:
  This touches execution and safety behavior. Port only with focused tests for
  rejected/accepted commands, PowerShell direct/script modes, rg glob handling,
  and unchanged approval/sandbox semantics.

6. Config explain, config requirements, MCP requirements, and coding budgets

Paths and pieces:
  config/src/config_explain.rs
  config/src/config_explain_tests.rs
  config/src/config_requirements.rs
  config/src/mcp_requirements.rs
  config/src/mcp_requirements_tests.rs
  config/src/coding_budgets.rs
  config/src/permissions_toml_tests.rs
  config/src/loader/project.rs
  config/src/config_document.rs
  config/src/strict_config.rs
  config/scripts/generate-proto.ps1

Why KD2 is better:
  Adds explainability around effective config, MCP requirements, strict/permission
  tests, and shared coding budget constants instead of scattered magic numbers.

Port caveats:
  Reconcile with current v0.143.0 config schema and loader behavior. If schema
  changes, regenerate config schema through the owning recipe, not by hand.

7. Session/evidence memory, dirty-worktree baseline, execution receipts, and
   working context brief

Paths and pieces:
  core/src/session_memory.rs
  core/src/session_memory/rendering.rs
  core/src/session_memory/command_classifier.rs
  core/src/dirty_worktree_baseline.rs
  core/src/execution_receipts.rs
  core/src/source_evidence.rs
  core/src/discovery_ledger.rs
  core/src/session/working_context_brief.rs
  core/src/session/working_context_brief_tests.rs
  core/src/session/turn_context.rs
  core/src/session/turn.rs

Why KD2 is better:
  Records freshness, validation evidence, dirty-worktree baselines, discovery
  ledger state, and compact working-context summaries. This helps prevent stale
  claims and missed validation in long-running local fork work.

Port caveats:
  Port after source/verification primitives, not before. This is broad model
  behavior and session-state behavior.

8. App-server startup config fallback repair

Paths and pieces:
  app-server/src/lib.rs
  app-server/tests/suite/v2/initialize.rs

Why KD2 is better:
  When preload config fails in a non-strict path, KD2 installs config/cloud/thread
  loaders from fallback config instead of leaving no-op bootstrap loaders active.

Port caveats:
  Validate initialization failure/fallback behavior and strict-mode behavior.

9. Structured turn-start validation and stable app-server error data

Paths and pieces:
  protocol/src/user_input.rs
  protocol/src/user_input_tests.rs
  app-server/src/error_code.rs
  app-server/src/request_processors/turn_processor.rs
  app-server/tests/suite/v2/turn_start.rs

Why KD2 is better:
  Adds text range validation and stable error codes/data for invalid environments,
  invalid thread settings, and invalid text elements.

Port caveats:
  Contract-visible. Update generated app-server schema only through generator if
  protocol output changes.

10. Remote-control enrollment and refresh lifecycle

Paths and pieces:
  app-server-transport/src/transport/remote_control/enroll.rs
  app-server-transport/src/transport/remote_control/websocket.rs
  app-server-transport/src/transport/remote_control/protocol.rs
  app-server-transport/src/transport/remote_control/segment.rs
  app-server-transport/src/transport/remote_control/tests.rs
  app-server-transport/src/transport/remote_control/tests/pairing_tests.rs

Why KD2 is better:
  Improves enrollment/refresh handling, Retry-After behavior, proactive refresh,
  status metadata, and redaction tests.

Port caveats:
  Runtime/security-sensitive. Port only with remote-control tests. Do not blindly
  delete or regress newer v0.143.0 transport helpers such as server_api.rs if
  present in the target fork.

11. State and persistence indexes

Paths and pieces:
  state/migrations/0039_thread_search_and_agent_job_indexes.sql
  state/logs_migrations/0003_feedback_log_covering_index.sql
  state/memory_migrations/0002_memory_selection_indexes.sql

Why KD2 is better:
  Adds indexes for job rows, cwd-normalized listing, feedback logs, and memory
  selection.

Port caveats:
  KD2's FTS thread_search table is not fully wired into query paths. Port only
  with query wiring, or accept it as storage/index overhead intentionally.

12. Rollout read-repair and rollout search snippets

Paths and pieces:
  rollout/src/state_db.rs
  rollout/src/search.rs
  rollout/src/list.rs
  rollout/src/compression_tests.rs

Why KD2 is better:
  The read-repair path loads full rollout items before reconciliation instead of
  reconciling from session metadata plus an empty item list. Search parses ripgrep
  JSON for snippets and handles compressed logical paths.

Port caveats:
  Keep v0.143.0's newer recency/history/world-state/session fields and thread-id
  fallback behavior. Cherry-pick only the repair/snippet ideas.

13. Memory generated-file atomic/idempotent writes

Paths and pieces:
  memories/write/src/generated_files.rs
  memories/write/src/storage.rs
  memories/write/src/workspace.rs

Why KD2 is better:
  Writes generated memory artifacts through temp file plus rename and skips
  unchanged byte-for-byte writes.

Port caveats:
  Keep newer v0.143.0 memory runtime/auth/config behavior. Port only the atomic
  generated-file writer.

14. TUI /diff untracked cap

Paths and pieces:
  tui/src/get_git_diff.rs

Why KD2 is better:
  Caps untracked file diffs at 50 and appends an omission notice/list. This
  prevents /diff from flooding context or spawning unbounded no-index diffs.

Port caveats:
  Small and likely safe, but verify TUI snapshot/output expectations.

15. /ideas slash command

Paths and pieces:
  tui/src/slash_command.rs
  tui/prompt_for_ideas_command.md

Why KD2 is better:
  Provides a non-mutating ranked-improvements command matching KD4's fork-local
  workflow for "what would you fix" and optimization-list requests.

Port caveats:
  Verify current slash dispatch, disabled-during-task behavior, and prompt asset
  packaging.

16. Prompt verification baseline and full-repo-audit review mode

Paths and pieces:
  prompts/src/verification.rs
  prompts/templates/verification/baseline.md
  prompts/src/review_request.rs
  prompts/src/review_request_tests.rs
  prompts/templates/review/full_repo_audit.md

Why KD2 is better:
  Stronger local agent discipline: prove wiring/reachability before claiming a
  runtime fix, and produce structured audit output with inspected/skipped files,
  validation, and remaining risk.

Port caveats:
  Model-visible prompt change. Port only the verification/audit pieces; KD2 also
  simplifies unrelated prompt templates.

17. Context fragment escaping and compaction

Paths and pieces:
  context-fragments/src/additional_context.rs
  context-fragments/src/fragment.rs
  context-fragments/tests/fragment_render.rs

Why KD2 is better:
  Renders explicit untrusted/application context tags, escapes attributes/text,
  preserves legacy external-context matching, and compacts repeated lines inside
  marked fragments.

Port caveats:
  Protocol ResponseItem field names may differ in the target fork. Adapt to the
  current protocol shape.

18. WSL/Windows path normalization

Paths and pieces:
  utils/path-utils/src/lib.rs
  utils/path-utils/src/env.rs
  utils/path-utils/src/path_utils_tests.rs

Why KD2 is better:
  Converts Windows drive paths to WSL mount paths, WSL UNC paths to POSIX, WSL
  mount paths back to Windows drive paths, and respects /etc/wsl.conf automount
  roots.

Port caveats:
  Validate on Windows and WSL if this is part of runtime behavior.

19. MCP compatibility fixes

Paths and pieces:
  mcp-server/src/approval_response.rs
  mcp-server/src/codex_tool_runner.rs
  mcp-server/src/lib.rs

Why KD2 is better:
  Parses both legacy {decision} and spec-shaped {action, content: {decision}}
  approval responses. Mirrors threadId into structured_content for clients that
  ignore content when structuredContent exists.

Port caveats:
  Reconcile with current app-server protocol/RMCP types.

20. Xcode MCP elicitation quirk centralization

Paths and pieces:
  app-server/src/client_quirks.rs
  app-server/src/request_processors/turn_processor.rs
  app-server/src/request_processors/thread_processor.rs

Why KD2 is better:
  Replaces duplicated Xcode 26.4 auto-deny helpers with one ClientQuirks rule.

Port caveats:
  Client-visible behavior. Keep compatibility tests around MCP elicitation.

21. Exact snapshot patch support

Paths and pieces:
  apply-patch/src/exact_snapshot.rs
  apply-patch/src/lib.rs

Why KD2 is better:
  Adds an exact patch snapshot renderer with line-ending detection, duplicate
  anchor checks, and overlap errors.

Port caveats:
  apply_patch is safety-sensitive. Do not weaken stale-read, preflight, or guard
  behavior. Validate with focused patch tests before using it.

MAYBE / review before porting
-----------------------------

1. ExecutionScope intent classifier

Paths:
  protocol/src/protocol.rs
  app-server-protocol/src/protocol/v2/turn.rs
  app-server/src/request_processors/intent_classifier.rs
  app-server/src/request_processors/turn_processor.rs

Reason:
  Useful anti-accidental-edit signal, but only valuable if core/model/tool gating
  honors it end to end.

2. Quiet command notification suppression

Paths:
  app-server/src/bespoke_event_handling.rs
  app-server/src/thread_state.rs
  app-server/src/bespoke_event_handling/turn_notifications.rs

Reason:
  Reduces UI noise for quick quiet commands. This changes client-visible event
  lifecycle and should be verified visually or through app-server tests.

3. Request-permissions path normalization

Paths:
  app-server/src/bespoke_event_handling.rs

Reason:
  Handles entry grants, rejects relative grants, and normalizes WSL/Windows-style
  paths. Safety-sensitive; requires runtime-critical validation.

4. Channel capacity constants

Paths:
  app-server/src/channel_defaults.rs
  app-server/src/in_process.rs
  app-server/src/transport.rs

Reason:
  Small cleanup to centralize channel defaults. Low risk, low priority.

5. Thread-store page-query helper

Paths:
  thread-store/src/local/page_query.rs
  thread-store/src/list_threads.rs
  thread-store/src/search_threads.rs

Reason:
  Refactor may be cleaner, but preserve v0.143.0's newer session/history/recency
  and relation semantics.

6. Rollout-trace reasoning audit

Paths:
  rollout-trace/src/model/audit.rs
  rollout-trace/src/model/mod.rs
  rollout-trace/src/reducer/mod.rs
  rollout-trace/src/event_handler_snapshot_tests.rs

Reason:
  Adds derived audit of failed tool calls and recovery hints. If ported, add
  direct assertions rather than relying only on snapshots.

7. Core-skills manager, diagnostics, and render budget pieces

Paths:
  core-skills/src/manager.rs
  core-skills/src/manager_tests.rs
  core-skills/src/diagnostics.rs
  core-skills/src/diagnostics_tests.rs
  core-skills/src/render.rs
  core-skills/src/loader.rs

Reason:
  Useful cache/diagnostic/render-budget ideas, but KD2 deletes newer environment
  root loader, service tests, and plugin snapshot support. Port ideas only.

8. TUI plugin popup and marketplace UX

Paths:
  tui/src/chatwidget/plugins.rs
  tui/src/app/event_dispatch.rs
  tui/src/app/plugin_mentions.rs

Reason:
  May include active-tab preservation, delayed loading shimmer, add/remove/upgrade
  prompts, and share helpers. Do not replace wholesale because KD2 deletes
  plugin_catalog.rs and many newer current features/snapshots.

9. Model catalog template hydration

Paths:
  models-manager/src/catalog_templates.rs
  models-manager/src/lib.rs
  models-manager/models.json
  models-manager/prompt.md

Reason:
  @codex-template:<field>:<slug> references reduce duplicated catalog prompt
  fields and reject unknown/nested references. Do not port KD2's forced default
  model behavior.

10. Agent task authorization target guard

Paths:
  agent-identity/src/lib.rs
  model-provider/src/auth.rs

Reason:
  Prevents signing a task assertion for a mismatched runtime. Do not port KD2's
  broader older agent-identity rollback.

11. External-agent overlong description guard

Paths:
  external-agent-migration/src/lib.rs

Reason:
  KD2 skips command skills over 1024 chars. v0.143.0 has a test preserving
  overlong descriptions, so verify the actual loader limit before changing it.

12. Collaboration templates

Paths:
  collaboration-mode-templates/templates/plan.md
  collaboration-mode-templates/templates/execute.md
  collaboration-mode-templates/templates/low_usage.md
  collaboration-mode-templates/templates/shared_editing_baseline.md
  collaboration-mode-templates/templates/pair_programming.md

Reason:
  Useful local workflow prompt assets, not runtime code.

13. App-server-test-client Windows process helper

Paths:
  app-server-test-client/src/process.rs
  app-server-test-client/src/lib.rs

Reason:
  Uses env::join_paths, temp dirs, netstat/taskkill on Windows instead of /tmp,
  lsof, kill, and ':' PATH concatenation. Port helper only; KD2 also deletes
  newer app-server-test-client features.

14. windows-sandbox-rs elevated stale-credential retry

Paths:
  windows-sandbox-rs/*

Reason:
  Could be useful if KD2 fixed a real elevated Windows sandbox issue. This is
  approval/sandbox-adjacent, so review current v0.143.0 first.

15. README, AGENTS, and SOURCE_MAP breadcrumbs

Paths:
  Many README.md additions across codex-rs
  AGENTS.md files under app-server-protocol, app-server, apply-patch, config,
  core, shell-command, shell-escalation, tools, tui
  SOURCE_MAP.md

Reason:
  Useful local navigation and ownership hints. Port selectively; do not overwrite
  better upstream documentation.

SKIP / do not port wholesale
----------------------------

1. Generated protocol/schema/snapshot/lockfile drift

Paths:
  app-server-protocol/schema/**
  core/config.schema.json
  **/*.snap
  Cargo.lock

Reason:
  Regenerate only after source changes through the owning recipes. Do not
  hand-port generated output.

2. Workspace and dependency downgrades

Reason:
  KD2 is older around workspace versioning and missing newer v0.143.0 members
  such as http-client and exec-server-protocol. Keep v0.143.0 structure.

3. Whole core-plugins rollback

Paths:
  core-plugins/src/manager.rs
  core-plugins/src/loader.rs
  core-plugins/src/manifest.rs
  core-plugins/src/marketplace.rs
  core-plugins/src/remote.rs
  core-plugins/src/store.rs
  deleted marketplace_policy.rs, marketplace_policy_tests.rs,
  marketplace_upgrade_tests.rs, npm_source.rs, npm_source_tests.rs

Reason:
  KD2 deletes a large amount of newer marketplace/NPM/policy behavior and tests.
  Keep v0.143.0. Only cherry-pick small diagnostics/render-budget ideas from
  core-skills if still useful.

4. Forced default model gpt-5.5

Paths:
  models-manager/src/manager.rs

Reason:
  KD2 bypasses normal configured/fallback model selection with a forced slug.
  Bad default for a new upstream-based fork unless explicitly requested and
  gated.

5. TUI reasoning/model popup simplifications

Paths:
  tui/src/chatwidget/reasoning_shortcuts.rs
  tui/src/chatwidget/model_popups.rs

Reason:
  v0.143.0 has richer model presets, advertised reasoning effort handling,
  custom levels, default/unset handling, high-effort warning, plan-mode scope
  prompt, and tests. KD2 reduces this.

6. Safety buffering, usage, and rate-limit reset removals

Paths:
  tui/src/chatwidget/safety_buffering.rs
  codex-api/src/safety_buffering.rs
  backend-client rate-limit reset types/tests

Reason:
  KD2 removes newer safety/retry/usage reset flows. Keep v0.143.0 unless there
  is a specific bug and a narrow fix.

7. Whole codex-api, backend-client, rmcp-client rollbacks

Reason:
  These contain useful custom CA call-site pieces, but broad KD2 diffs remove
  newer safety buffering, rate-limit reset, OAuth/startup, and auth-status code.
  Port only the custom CA transport integration.

8. Login and model-provider auth shrink

Paths:
  login/src/auth/*
  login/src/outbound_proxy.rs
  model-provider/src/auth.rs
  Bedrock-related files

Reason:
  KD2 removes substantial newer auth/provider code. Treat as older rollback.

9. Broad connectors rollback

Paths:
  connectors/src/app_info.rs
  connectors/src/plugin_config.rs
  connectors/src/plugin_config_tests.rs
  connectors/src/snapshot.rs
  connectors/src/snapshot_tests.rs
  connectors/src/filter.rs
  connectors/src/lib.rs

Reason:
  Removes newer app/plugin snapshot configuration surfaces.

10. Broad app-server protocol item split/removals

Paths:
  app-server-protocol/src/protocol/v2/item*.rs
  protocol/src/items.rs

Reason:
  KD2 loses richer v0.143.0 variants and app context. Only port explicit runtime
  metadata or validation fields if current source needs them.

11. Broad state/thread-store rollback

Reason:
  KD2 lacks v0.143.0 recency_at, ThreadHistoryMode, relation filters, selected
  capability roots, and newer session/context fields. Port only targeted indexes
  or helper ideas.

12. Broad rollout rollback

Reason:
  KD2 removes newer history/recency/session/world-state/compression behavior.
  Port only read-repair and search-snippet ideas.

13. Broad rollout-trace rollback

Reason:
  KD2 removes v0.143.0 exec trace path serialization compatibility. Port only
  reasoning_audit if useful.

14. Broad memories runtime rollback

Reason:
  KD2 is older around model client/start options/auth/config snapshot. Port only
  atomic generated-file writes and maybe prompt wording.

15. Broad code-mode/code-mode-host/code-mode-protocol rollback

Reason:
  KD2 removes newer session runtime, remote provider/session, cell actor, V8/JIT,
  and tests. Skip unless a very narrow KD2-only bug fix is identified later.

16. Current-time API

Reason:
  v0.143.0 already has the app-server current-time API and protocol shape. KD2
  differences are mostly import/style or generated drift. Do not port.

17. Multi-agent/history/rollback/thread controls

Reason:
  v0.143.0 already has ThreadHistoryMode, multi_agent_mode, initialTurnsPage,
  pending rollbacks, and rollout truncation support. Some v0.143.0 versions are
  richer than KD2. Do not overwrite with KD2.

18. Plugin tool suggestion metadata wholesale

Reason:
  v0.143.0 already has tool suggestion metadata. KD2 changes are mixed with
  older plugin manager/loader shapes and should not replace current code.

19. Skill sample changes

Paths:
  skills/src/assets/samples/*

Reason:
  Mostly README breadcrumbs and reductions to plugin-creator samples/validators.
  No clear better runtime behavior.

Suggested port order
--------------------
1. Small low-risk user-facing wins:
   /diff untracked cap, /ideas command, selected README/AGENTS/SOURCE_MAP notes.

2. Local fork proof chain:
   build_info, local runtime provenance, app-server initialize metadata, CLI
   doctor/runtime output.

3. Network/client support:
   custom CA transport and only the required HTTP/WebSocket call-site wiring.

4. Local validation discipline:
   source tools, verify_local scripts/tool, shell output summary, session evidence
   memory, dirty-worktree baselines.

5. Command execution hardening:
   command preflight, command shape, PowerShell script-body handling, rg linting.

6. Config diagnostics:
   config explain, mcp requirements, permission tests, coding budget constants.

7. App-server protocol/runtime improvements:
   startup fallback, structured turn-start validation, client quirks, targeted
   protocol fields with generated schema regeneration.

8. Persistence/runtime targeted fixes:
   state indexes, rollout read repair, rollout search snippets, memory atomic
   writes, cloud task override propagation, app-server-test-client Windows helper.

9. Remote-control enrollment:
   port only after a focused test plan because it is runtime/security-sensitive.

Validation requirements for actual port work
--------------------------------------------
- Rust crate/source changes: run focused cargo/just checks for touched crates.
- Protocol or app-server schema changes: regenerate schema through the owning
  recipe and run focused app-server protocol tests.
- Config schema changes: regenerate config schema through the owning recipe.
- Safety/exec/sandbox/apply-patch changes: run focused accepted/rejected path
  tests and do not change unrelated approval/sandbox semantics.
- Desktop-visible changes: rebuild/publish local codex, restart Desktop, and
  prove the runtime chain with current process path, binary hash/version, and
  app-server initialize metadata.

Bottom line
-----------
KD2 contains real local improvements worth preserving, especially local runtime
proof, custom CA support, source/verification tooling, command preflight,
config diagnostics, memory/evidence discipline, and selected app-server fixes.

KD2 also contains many older rollbacks relative to v0.143.0. Do not copy KD2
wholesale. Port narrowly by feature slice, keep v0.143.0's newer behavior where
it is richer, and regenerate generated outputs only from source changes.

Harnessed #5-#15 implementation status (2026-07-09)
---------------------------------------------------

Scope:
  Used the KD4 harness workflow for audit items #5 through #15. Validation below
  is from the KD4 checkout at C:\Users\kuh\Desktop\kd4.

Implemented or confirmed:
  #5 command preflight diagnostics, command shape, PowerShell/rg guidance, and
  shell output summaries are present and wired. The shell output summary tests
  were corrected to cross the configured default summary threshold.

  #6 config explain and coding budget constants are present and tested.

  #8 app-server startup config fallback repair is present. Added a regression
  test for non-strict preload failure warnings and fallback config behavior.

  #9 structured turn-start validation and stable app-server error data are
  present. App-server schema fixtures were regenerated through the owning
  `just app-server-schema-check` recipe and then verified stable.

  #10 remote-control enrollment/refresh lifecycle is present in KD4's newer
  server_api/enroll/websocket split. Kept KD4's newer structure and validated
  Retry-After, proactive/required refresh, unauthorized recovery, and token
  preservation paths.

  #11 state/log/memory indexes are added. KD2's unwired FTS thread_search table
  was intentionally not ported.

  #12 rollout read-repair and search snippets are ported. Read-repair now loads
  full rollout items before reconciliation. A cwd normalization bug found during
  validation was fixed so loaded SessionMeta cwd values do not make repaired
  SQLite rows invisible to normalized cwd filters.

  #13 memory generated-file writes are atomic/idempotent through a generated
  file helper, and storage sync/rebuild paths use it.

  #14 TUI `/diff` caps untracked file diffs at 50 and lists omitted paths.

  #15 `/ideas` slash command is wired through command metadata, dispatch, prompt
  asset, scoped prompt arguments, queue-drain behavior, and popup exact-match
  selection.

Blocked:
  #7 full session/evidence memory, dirty-worktree baseline, execution receipts,
  source evidence, discovery ledger, and working context brief is not complete.
  A dedicated feasibility pass found KD2's #7 depends on source-tool and
  source-citation surfaces that are not in current KD4, including
  `tools/handlers/source.rs`, `path_aware_search.rs`, `protocol::source_citation`,
  and `Feature::SourceTools`. It also collides with KD4's newer WorldState and
  StepContext model-input path. Do not claim #7 complete until the source-tool
  slice is ported and #7 is implemented as its own runtime-critical design.

Validation run:
  cargo test -p codex-shell-command rg --lib
  cargo test -p codex-core command_preflight --lib
  cargo test -p codex-core command_shape --lib
  cargo test -p codex-core shell_output_summary --lib
  cargo test -p codex-config config_explain --lib
  just config-schema-check
  cargo test -p codex-protocol user_input --lib
  cargo test -p codex-app-server --test all initialize_reports_non_strict_config_preload_failure
  cargo test -p codex-app-server --test all turn_start_rejects_invalid_text_element_range_before_starting_turn
  cargo test -p codex-app-server --test all turn_start_rejects_invalid_permission_selection_before_starting_turn
  cargo test -p codex-app-server --test all turn_start_rejects_unknown_environment_before_starting_turn
  just app-server-schema-check
  cargo test -p codex-app-server-transport server_api --lib
  cargo test -p codex-app-server-transport refresh --lib
  cargo test -p codex-state migrations --lib
  cargo test -p codex-rollout state_db --lib
  cargo test -p codex-rollout search_rollout_matches_fallback_covers_plain_and_compressed_rollouts --lib
  cargo test -p codex-memories-write generated_files --lib
  cargo test -p codex-memories-write sync_rollout_summaries_and_raw_memories_file_keeps_latest_memories_only --lib
  cargo test -p codex-tui get_git_diff_caps_untracked_file_diffs_and_lists_omitted_paths --lib
  cargo test -p codex-tui slash_ideas --lib
  cargo test -p codex-tui selecting_ideas_by_exact_match --lib
  python C:\Users\kuh\Desktop\LOCAL-KD\plugins\cache\local-wiring-guards\wiring-guard\0.1.10\runtime\wiring_guard.py --session-id kd4-port-5-15-finish check --ledger --ledger-mode summary --intent-only
  git diff --check

Harnessed #1-#15 double-check excluding #7 (2026-07-09)
--------------------------------------------------------

Scope:
  Used the updated KD4 harness workflow for audit items #1 through #15,
  excluding #7 by request. Evidence artifacts are under:
  .codex/harness/runs/2026-07-09-port-1-15-double-check/

Confirmed with current KD4 source and validation:
  #1 local runtime provenance is wired through app-server initialize metadata
  and CLI build-info/version proof. Focused validation included
  `cargo test -p codex-cli long_version_lists_build_stamp_fields --bin codex`
  and app-server initialize metadata coverage.

  #2 custom CA support is implemented in `codex-rs/http-client`, shared by
  HTTP and WebSocket transports, and keeps KD4's newer transport structure.
  Focused validation covered unit tests, CA env integration tests, and
  WebSocket dialer tests.

  #4 verify_local is implemented, registered only for eligible local repo
  markers, and records proof-bearing validation. Rust handler/spec-plan tests
  and `scripts.test_verify_local` passed.

  #5 command preflight, command shape, PowerShell/rg guidance, and shell output
  summaries are present on shell/unified-exec paths. Focused command tests
  passed.

  #6 config explain, requirements, and coding budgets match current config
  schema. `just config-schema-check` passed after a solo rerun; the earlier
  parallel attempt timed out rather than failing semantically.

  #8 app-server startup fallback and #9 turn-start structured validation are
  covered by focused app-server/protocol tests. `just app-server-schema-check`
  passed with generated outputs already up to date.

  #10 remote-control refresh/enrollment behavior is covered by server API and
  refresh tests.

  #11 targeted state/log/memory indexes are present. KD2's unwired FTS
  `thread_search` table is still intentionally absent.

  #12 rollout read-repair/search snippets, #13 memory generated-file
  atomic/idempotent writes, #14 TUI `/diff` untracked cap, and #15 `/ideas`
  all passed focused owner tests.

Not implemented / not claimed:
  #3 source discovery/source evidence/path-aware search is not implemented in
  this KD4 checkout. The expected KD2 files are absent:
  `core/src/tools/handlers/source.rs`, `source_spec.rs`,
  `source_evidence.rs`, `path_aware_search.rs`, `workspace_index.rs`,
  `repo_guidance_evidence.rs`, and `file-search/src/source_search.rs`.
  Search found only unrelated MCP resource code and one `read_file_span` string
  in turn-diff tracking. Treat #3 as the next separate source-tool slice, not
  as complete.

  #7 was excluded by request and remains unclaimed.

Fixes made during this double-check:
  `codex-rs/core/src/tools/runtimes/shell/unix_escalation.rs` now imports
  `UnifiedExecRequest` directly for the unified-exec zsh-fork helper.
  `codex-rs/tui/src/chatwidget/slash_dispatch.rs` now has an explicit no-args
  `SlashCommand::Ideas` arm in inline slash dispatch so `/ideas` is not left to
  the wildcard fallback.

Final validation:
  Focused Rust and script tests listed in the harness evidence file passed.
  `git diff --check` exited 0 with only LF-to-CRLF normalization warnings.
  Wiring Guard session `kd4-port-1-15-double-check` passed in scoped
  intent-only mode with `Verdict: WIRED`.

Desktop visibility:
  No local publish or Codex Desktop restart was performed in this pass. Any
  desktop-visible claim still requires local publish/restart proof from this
  checkout.

Harnessed #1-#15 double-check excluding #3 and #7 (2026-07-09)
--------------------------------------------------------------

Scope:
  User requested a mandatory subagent-assisted double check for #1 through #15,
  explicitly ignoring #3 and #7. Evidence artifacts are under:
  .codex/harness/runs/2026-07-09-port-1-15-excluding-3-7-subagent-audit/

Subagents:
  Subagents were attempted for app/protocol/transport and core command/config.
  The current agent pool prevented a third fresh subagent, and the started
  subagents stalled or were interrupted without returning usable findings. The
  final closure is therefore based on root source review, closure sweeps,
  focused validation, and scoped Wiring Guard proof.

Confirmed included scope:
  #1, #2, #4, #5, #6, #8, #9, #10, #11, #12, #13, #14, and #15 were reviewed
  against current KD4 source. #3 and #7 remain intentionally excluded and are
  not claimed complete.

Fix made:
  #13 had one confirmed replacement-safety issue. The memory generated-file
  writer now uses `tempfile::NamedTempFile::new_in(parent)`, flushes the temp
  file, and persists it to the target path in `spawn_blocking`. Storage paths
  for raw memory rebuilds and rollout summary generation call this helper.
  Added `write_generated_file_replaces_existing_file` regression coverage and
  moved `tempfile` to normal dependencies for `codex-memories-write`.

Validation:
  Focused Rust/script/schema validation passed for app-server initialize and
  turn-start validation, custom CA HTTP/WebSocket transport, verify_local,
  command preflight/shape/output summary, config explain/MCP requirements/
  budgets, state migrations, rollout state/search, memories write, TUI `/diff`,
  and `/ideas`. `just app-server-schema-check`, `just config-schema-check`,
  `python -m unittest scripts.test_verify_local`, and `git diff --check` passed.
  `git diff --check` only printed LF-to-CRLF normalization warnings.

Wiring:
  Scoped Wiring Guard session
  `kd4-port-1-15-excluding-3-7-subagent-audit` passed with `Verdict: WIRED` for
  resolvable changed targets. Direct `rg` source proof records the #13 runtime
  chain from `phase2.rs` to `storage.rs` to `generated_files.rs`.

Desktop visibility:
  No local publish or Codex Desktop restart was performed in this audit pass.
